{
  "name": "vba-extension",
  "publisher": "tagi0",
  "displayName": "VBA Extension",
  "version": "0.1.0",
  "private": true,
  "main": "dist/extension.js",
  "description": "VS Code language support for Excel VBA.",
  "categories": [
    "Programming Languages"
  ],
  "files": [
    "dist/**",
    "snippets/**",
    "syntaxes/**",
    "language-configuration.json",
    "resources/**",
    "README.md"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "activationEvents": [
    "onCommand:vba.extract",
    "onCommand:vba.combine",
    "onLanguage:vba"
  ],
  "contributes": {
    "commands": [
      {
        "command": "vba.extract",
        "title": "VBA: Extract Source with vbac"
      },
      {
        "command": "vba.combine",
        "title": "VBA: Combine Source with vbac"
      }
    ],
    "languages": [
      {
        "id": "vba",
        "aliases": [
          "Excel VBA",
          "VBA"
        ],
        "extensions": [
          ".bas",
          ".cls",
          ".frm"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "vba",
        "scopeName": "source.vba",
        "path": "./syntaxes/vba.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "vba",
        "path": "./snippets/vba.code-snippets"
      }
    ],
    "configuration": {
      "title": "Excel VBA",
      "properties": {
        "vba.analysis.debounceMs": {
          "type": "number",
          "default": 300,
          "minimum": 50,
          "maximum": 2000,
          "description": "Debounce delay in milliseconds before the language server re-analyzes a VBA document."
        },
        "vba.analysis.logPerformance": {
          "type": "boolean",
          "default": false,
          "description": "Log VBA analysis timing and document-size metrics to the language server console without logging source text or absolute file paths."
        }
      }
    }
  },
  "keywords": [
    "vba",
    "excel",
    "office"
  ],
  "scripts": {
    "build": "node build.mjs && node ../../scripts/prepare-extension.mjs",
    "test": "npm run build --workspace @vba/core && npm run build --workspace @vba/server && npm run build --workspace vba-extension && if exist dist-test rmdir /s /q dist-test && tsc -p tsconfig.test.json && node dist-test/test/runTest.js",
    "lint": "tsc --noEmit --rootDir .. -p tsconfig.json",
    "clean": "if exist dist rmdir /s /q dist && if exist dist-test rmdir /s /q dist-test",
    "package": "vsce package --no-dependencies --allow-missing-repository --skip-license -o ../../dist/vba-extension.vsix"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
