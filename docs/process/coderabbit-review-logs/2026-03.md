# CodeRabbit レビュー記録 2026-03

2026-03 の PR ごとの記録。運用判断や手順の正本には使わず、CodeRabbit 運用は [../coderabbit-review.md](../coderabbit-review.md) を参照する。

## 2026-03-22 PR #94 test: workbook root family の shared case spec を抽出
- レビュー状況: `COMMENTED（対応済み・review fix push 後は CodeRabbit: Review skipped / Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは 3 点出た。1 点目は server の `shape` completion slice が shared case spec から十分に拾えていない点、2 点目は `Application` workbook root family の OLE negative slice が merge 前に空でも見逃せる点、3 点目は shared case 抽出 helper が空配列でもそのまま後段へ流せる点だった。いずれも妥当だったため、PR 作成前に shared case の scope 補正と per-slice guard を追加して解消し、再レビューでは P0-P3 指摘なしを確認した。
  - CodeRabbit は 3 論点を出した。1 件目は extension test の `WorkbookRootFamilyCompletionEntry` が `route` 必須の単一型で、negative entry に `route` が無い shared CJS との差分を `as` キャストで隠している点、2 件目は server test の broad root / application root slice が merge 後の配列しか検証しておらず、個別 slice が空でも通ってしまう点、3 件目は `Application.Range("A1").Shapes("CheckBox1").OLEFormat.Object` 系の negative case が `shape` scope を欠いていて shape 系回帰から外れている点だった。
  - 3 件とも採用し、extension 側は positive / negative completion entry を分割して `route` を正例だけの契約へ狭め、server 側は broad root direct/item と application OLE/shape の completion・semantic slice を `requireSharedWorkbookRootEntries()` で個別に fail-fast させ、shared CJS 側は `Application.Range("A1").Shapes...` negative の completion / semantic に `server-application-shape` を追加した。
  - review fix push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメント `Reviews paused.` と `CodeRabbit: Review skipped` を確認した。検証は `npm run test --workspace @vba/server`、`npx tsc -p packages/extension/tsconfig.test.json`、`npm run lint`、`npm run package` を実行した。`npm run test --workspace vba-extension` は実行中の Code インスタンス制約により今回もコマンドライン実行していない。
- 指摘一覧:
  - [採用] `reviewer`: shared case の `shape` 正例 / 負例 slice が server 側で十分に抽出されていない。
    理由: shared spec を正本にするなら、adapter 側で scope 取り違えをした時点で即失敗しないと drift を見逃すため。
  - [採用] `reviewer` / CodeRabbit: merge 後配列だけでなく per-slice で空配列を reject するべき。
    理由: `static + matched` や `ole + shape` の片側だけが空でも、合算後に他方が残っていると regression が隠れるため。
  - [採用] CodeRabbit: completion entry 型は positive / negative を分け、`route` を正例専用の契約にするべき。
    理由: shared CJS の negative entry に `route` が無い以上、単一型 + `as` キャストでは future edit 時に unsafe read を防げないため。
  - [採用] CodeRabbit: `Application.Range("A1").Shapes(...).OLEFormat.Object` negative は `shape` scope も持つべき。
    理由: `shape` family の closed path として監視したいケースが OLE 系だけへ分類されると、shape adapter の取りこぼしを検知できないため。
- この作業で当てはまりそうな内容（横展開候補）:
  - shared case spec を JS/CJS で持つときは、adapter 側 TypeScript で positive / negative の型境界を分け、`as` キャストで欠落プロパティを飲み込まない方が安全。
  - shared matrix を複数 slice から merge するテストは、最終配列の non-empty assert だけでは不十分で、slice 抽出直後の fail-fast guard を共通 helper で入れた方が drift を早く見つけられる。
  - `scope` は単なる分類ラベルではなく test routing の契約なので、`OLEFormat.Object` のように見た目が似た経路でも `ole` / `shape` の両 family へ入るべきかを review 時に確認した方がよい。
  - canonical case spec を repo root に置く場合、package-local adapter 側の filter 条件と shared spec の scope 名が 1 文字でもずれると coverage が silently shrink するため、`requireShared...` のような非空 guard を早めに整備した方が保守しやすい。
- 実施:
  - [packages/extension/test/suite/index.ts](../../packages/extension/test/suite/index.ts) で `WorkbookRootFamilyCompletionEntry` を `Base` / `Positive` / `Negative` へ分割し、`getSharedWorkbookRootCompletionEntries()` の overload と positive-only mapper を追加した。
  - [packages/server/test/documentService.test.js](../../packages/server/test/documentService.test.js) で broad root direct/item、application OLE/shape の completion / semantic slice ごとに `requireSharedWorkbookRootEntries()` を通し、空配列を即 fail させた。
  - [test-support/workbookRootFamilyCaseTables.cjs](../../test-support/workbookRootFamilyCaseTables.cjs) で `Application.Range("A1").Shapes...` negative の completion / semantic scope へ `server-application-shape` を追加した。
  - 検証として `npm run test --workspace @vba/server`、`npx tsc -p packages/extension/tsconfig.test.json`、`npm run lint`、`npm run package` を実行した。
- 残課題:
  - 次段では workbook root family の hover / signature anchor も shared case spec 化するかを再評価し、completion / semantic と同じく package-local adapter で十分かを見直す。

## 2026-03-21 PR #84 test: unqualified worksheet broad root の Item 回帰を拡張
- レビュー状況: `COMMENTED（対応済み・review fix push 後は CodeRabbit: Review skipped / Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、`Worksheets("SheetName")` / `Application.Worksheets("SheetName")` 配下の `.OLEObjects.Item("ShapeName").Object` と `.Shapes.Item("ShapeName").OLEFormat.Object` を broad root family の正例として固定しつつ、root `.Item("SheetName")` 形式は v1 非対象として閉じる方針は妥当と確認された。
  - 残留リスクとして 2 点だけ出た。1 点目は root `.Item("SheetName")` 非対象が extension 側では hover / signature 中心で、completion も明示負例にした方が安全という点、2 点目は broad root test matrix の重複が増えており helper 化の余地がある点だった。前者はこの PR で採用し、後者は `TASKS.md` の次候補に残した。
  - CodeRabbit の actionable は無く、nit は 1 件だけだった。内容は extension test の negative completion assertion に失敗メッセージが無く、原因特定が遅れやすい点で、対象箇所へ説明メッセージを追加して採用した。
  - review fix push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメント `Reviews paused.` と `Review skipped` を確認した。最終的に `quality-check` / `danger-check` / `label` / `CodeRabbit` はすべて pass した。
- 指摘一覧:
  - [採用] `reviewer`: root `.Item("SheetName")` 非対象は extension 側でも completion を明示負例化した方が安全。
    理由: hover / signature だけでは、completion 経路だけが誤って開く退行を見逃す余地があるため。
  - [採用] CodeRabbit nit: negative completion assertion に失敗メッセージを付けるべき。
    理由: broad root matrix が増えるほど、どの root / selector が漏れたのかを assertion message で直ちに判別できた方が保守しやすいため。
  - [非採用] `reviewer` の broad-root helper 化をこの PR で行う。
    理由: 今回は item selector 回帰の固定を優先し、test helper 抽出まで広げると差分の責務がぼやけるため。次候補として `TASKS.md` へ分離した。
- この作業で当てはまりそうな内容（横展開候補）:
  - broad root の非対象境界は hover / signature だけでなく completion も負例化した方が、partial regression を早く見つけやすい。
  - matrix 型の negative assertion は、`false` / `undefined` を見るだけでも失敗メッセージを添えた方が review と保守の速度が上がる。
  - root call form と root `.Item(...)` のように見た目が近い構文は、docs で境界を明示し、fixture でも両方置いて差分を固定した方が誤拡張を防ぎやすい。
  - broad root family の test matrix が増えてきたら、matched / mismatched / unavailable / shadow / root `.Item` を helper へ寄せる準備を早めに始めた方がよい。
- 実施:
  - `packages/server/test/documentService.test.js` に、unqualified broad root の `.OLEObjects.Item("CheckBox1").Object` / `.Shapes.Item("CheckBox1").OLEFormat.Object` 正例と root `.Item("Sheet One")` 非対象を固定する新規テストを追加した。
  - `packages/extension/test/fixtures/WorksheetBroadRootBuiltIn.bas` と `packages/extension/test/suite/index.ts` に、broad root item selector の completion / hover / signature help 正例、root `.Item` completion / hover / signature help 負例、mismatch snapshot 負例を追加した。
  - ADR [0005](../adr/0005-explicit-sheet-name-root-policy.md) と [explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md) に、`Worksheets.Item("SheetName")` / `Application.Worksheets.Item("SheetName")` は v1 broad root 対象外であることを追記した。
  - `TASKS.md` を更新し、本タスク完了と次候補 `broad root family の重複テスト補助を整理する` を追加した。
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行し、review fix 後は `npm run test --workspace vba-extension` を追加で通した。
- 残課題:
  - 次段では broad root family の server / extension test matrix を helper 化し、matched / mismatched / unavailable / shadow / root `.Item` 非対象の追加ケースを差分少なく書けるようにする。

## 2026-03-21 PR #83 feat: unqualified worksheet broad root を最小接続
- レビュー状況: `COMMENTED（対応済み・review fix push 後は CodeRabbit: Review skipped / Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、`Worksheets("SheetName")` / `Application.Worksheets("SheetName")` を matched active workbook binding 下だけ synthetic `Worksheet` root へ落とす境界、`Sheets` / `ActiveSheet` / numeric / dynamic selector を broad root family から除外する方針、`Worksheets` shadow を尊重して broad root を閉じる方針は妥当と確認された。
  - 残留リスクとして 3 点だけ出た。1 点目は `Application` shadow の server 回帰が未固定だった点、2 点目は extension 側で `available` だが workbook mismatch のときに broad root が閉じることを E2E で固定していなかった点、3 点目は `.Item("...")` 粒度が次段へ残っている点だった。前 2 点は今回の PR で採用し、後者は次候補として `TASKS.md` へ残した。
  - CodeRabbit の actionable は 1 件で、server test が `Application.Worksheets(1)` / `Application.Worksheets(GetIndex())` の negative と、`available` だが non-matching workbook snapshot の negative を十分に押さえていない点だった。これは broad root family の root spelling を増やしたときの横展開漏れとして妥当だったため採用し、同一テストへ `Application.Worksheets` 側の numeric / dynamic selector と mismatch snapshot の negative を追加した。
  - nit として挙がった `Worksheets.Item("Sheet One")` / `Application.Worksheets.Item("Sheet One")` の broad root coverage は、今回の v1 公開境界が `Worksheets("...")` / `Application.Worksheets("...")` に限られるためこの PR では非採用とし、次候補 `unqualified worksheet broad root の Item selector 回帰を拡張する` に昇格した。
  - review fix push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメント `Reviews paused.` と `Review skipped` を確認した。最終的に `quality-check` / `danger-check` / `label` / `CodeRabbit` はすべて pass した。
- 指摘一覧:
  - [採用] `reviewer`: `Application` shadow case でも broad root を閉じる server 回帰を追加するべき。
    理由: unqualified `Worksheets` と `Application.Worksheets` を同じ family として開く以上、root-specific な shadow 負例も両方そろえないと誤昇格を検知しきれないため。
  - [採用] `reviewer`: extension E2E で matched snapshot だけでなく mismatch snapshot も固定するべき。
    理由: broad root gating は `unavailable` だけでなく `available but mismatched` でも閉じることが契約の中心で、runtime identity 系の退行は E2E で押さえておいた方が安全なため。
  - [採用] CodeRabbit: `Application.Worksheets` 側の numeric / dynamic selector と mismatch snapshot negative を server test に足すべき。
    理由: broad root family に複数の spelling がある場合、positive だけでなく non-target selector も spelling ごとに固定しないと、片系統だけが緩む退行を見逃しやすいため。
  - [非採用] CodeRabbit nit: `Worksheets.Item("Sheet One")` / `Application.Worksheets.Item("Sheet One")` も同 PR で broad root family に含めるべき。
    理由: 今回の v1 境界は literal call form の `Worksheets("...")` と `Application.Worksheets("...")` に限定しており、root `.Item(...)` を開くかどうかは `.OLEObjects.Item(...)` / `.Shapes.Item(...)` の回帰整理と合わせて次段で判断した方が境界を保ちやすいため。
- この作業で当てはまりそうな内容（横展開候補）:
  - broad root family を追加するときは、`matched available` / `mismatched available` / `unavailable` の 3 状態を server test と extension E2E の両方で押さえた方が runtime identity 系の回帰を拾いやすい。
  - `Worksheets` / `Application.Worksheets` のように root spelling が複数ある built-in family は、positive だけでなく numeric / dynamic / shadow の negative も spelling ごとに固定した方が安全。
  - broad root routing が root symbol 名に依存する場合、shadow test は family 全体で root-specific に用意した方が partial regression を見つけやすい。
  - 未対応スコープの `.Item(...)` 形は「今は閉じている」こと自体を早めに負例化しておくと、後続 PR で境界を広げたときの差分が読みやすい。
- 実施:
  - `packages/server/src/lsp/documentService.ts` で `Worksheets("...")` / `Application.Worksheets("...")` を matched active workbook binding 下だけ synthetic `Worksheet` root へ正規化し、worksheet control sidecar を `sheetName` で join する処理を追加した。
  - `packages/server/test/documentService.test.js` に matched / mismatched / unavailable snapshot、`Worksheets` shadow、`Application` shadow、`Application.Worksheets(1)` / `Application.Worksheets(GetIndex())` negative を追加した。
  - `packages/extension/test/fixtures/WorksheetBroadRootBuiltIn.bas` と `packages/extension/test/suite/index.ts` に broad root 正例・負例・mismatch E2E を追加した。
  - `TASKS.md` を更新し、本タスク完了と次候補 `unqualified worksheet broad root の Item selector 回帰を拡張する` を反映した。
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行して通過を確認した。
- 残課題:
  - 次段では `Worksheets("SheetName").OLEObjects.Item("ShapeName").Object` と `Application.Worksheets("SheetName").Shapes.Item("ShapeName").OLEFormat.Object` を broad root family へ横展開し、root `.Item("SheetName")` を同 family に含めるかを同時に整理する。

## 2026-03-21 PR #82 docs: unqualified worksheet broad root の方針を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の初回レビューでは 3 点出た。1 点目は `TASKS.md` では `ActiveWorkbook.Worksheets("SheetName")` broad root が実装済みなのに、[explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md) が still closed と読める点、2 点目は unqualified broad root の対象構文境界が `Worksheets("...")` だけなのか `Application.Worksheets("...")` も含むのか未固定だった点、3 点目は `Worksheets` shadow case が未定義だった点だった。
  - いずれも妥当だったため、ADR [0005](../adr/0005-explicit-sheet-name-root-policy.md) と [explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md) を更新し、`ActiveWorkbook.Worksheets("SheetName")` は workbook-bound gating 済み、unqualified `Worksheets("SheetName")` / `Application.Worksheets("SheetName")` は同じ active-workbook family として将来同条件で扱う、`Sheets` / `ActiveSheet` / numeric / dynamic / grouped / shadow case は broad root family から除外する、という境界を正本へ集約した。
  - 追加で、[active-workbook-identity-provider-contract.md](active-workbook-identity-provider-contract.md)、[workbook-binding-manifest-feasibility.md](workbook-binding-manifest-feasibility.md)、[shape-oleformat-object-explicit-sheet-root-feasibility.md](shape-oleformat-object-explicit-sheet-root-feasibility.md)、[shape-oleformat-object-promotion-feasibility.md](shape-oleformat-object-promotion-feasibility.md)、[README.md](README.md)、[TASKS.md](../../TASKS.md) の古い表現もそろえ、次候補を `unqualified worksheet broad root の最小接続` に更新した。
  - 再レビューでは `reviewer` から P0-P3 指摘なしとなり、残留リスクは「過去時点の feasibility 文書を日付注記なしに抜粋すると誤読余地がある」だけだったため、`この文書作成時` の注記で補足した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、ADR `0005`、各 process 文書を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer`: `ActiveWorkbook` 実装済み境界と feasibility 文書の still-closed 記述を揃えるべき。
    理由: docs 間で実装済み / 未実装の認識がずれると、次タスクの判断が古い前提へ引き戻されるため。
  - [採用] `reviewer`: unqualified broad root の対象構文に `Application.Worksheets("...")` を含むかを正本で固定するべき。
    理由: Office VBA の一次情報では unqualified `Worksheets` が active workbook family であり、`Application.Worksheets` と切り分けない方が自然なため。
  - [採用] `reviewer`: `Worksheets` shadow case を broad root family の除外境界として明記するべき。
    理由: built-in と user-defined symbol の優先順位が曖昧だと誤補完リスクが残るため。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - feature 実装後に docs タスクへ戻るときは、`TASKS.md` と feasibility 文書の「現行境界」が食い違っていないかを最初に確認した方がよい。
  - unqualified built-in root の方針を決めるときは、暗黙形だけでなく `Application.<same root>` を同じ family として扱うかを同時に決めた方が後続実装がぶれにくい。
  - broad root family を開く議論では、positive path だけでなく `Sheets` / `ActiveSheet` / grouped / shadow のような non-target を先に固定した方が review が短く済む。
  - feasibility 文書が過去時点の判断を含む場合は、`この文書作成時` のような注記を入れて current policy と historical note を分けた方が誤読を減らせる。
- 実施:
  - ADR [0005](../adr/0005-explicit-sheet-name-root-policy.md) を更新し、`ActiveWorkbook` 実装済み境界、unqualified / `Application.Worksheets` の同 family 扱い、non-target selector と shadow case の除外を明記した。
  - [explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md) を更新し、公式ソース、現行境界、将来開く条件、除外境界、次候補を現行 state に合わせて整理した。
  - [active-workbook-identity-provider-contract.md](active-workbook-identity-provider-contract.md)、[workbook-binding-manifest-feasibility.md](workbook-binding-manifest-feasibility.md)、[shape-oleformat-object-explicit-sheet-root-feasibility.md](shape-oleformat-object-explicit-sheet-root-feasibility.md)、[shape-oleformat-object-promotion-feasibility.md](shape-oleformat-object-promotion-feasibility.md)、[README.md](README.md)、[TASKS.md](../../TASKS.md) を更新した。
  - docs-only のため build / lint / test は実行していない。
- 残課題:
  - 次段では `Worksheets("SheetName")` / `Application.Worksheets("SheetName")` を `ActiveWorkbook.Worksheets("SheetName")` と同じ helper で最小接続し、`OLEObject.Object` / `Shape.OLEFormat.Object` の両経路で負例をそろえる。

## 2026-03-21 PR #81 feat: workbook bound broad root gating を追加
- レビュー状況: `COMMENTED（対応済み・review fix push 前）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、`ActiveWorkbook.Worksheets("SheetName")` broad root を `available snapshot + workbook-binding.json match` に限定して開く境界、`OLEObject.Object` / `Shape.OLEFormat.Object` への既存 control owner 導線へだけ接続する粒度、`TASKS.md` の次候補を unqualified broad root 判定へ進める流れは妥当と確認された。
  - 残留リスクとして 2 点だけ出た。1 点目は host bridge notification が server へ反映されるまでの伝播遅延を test harness が十分に待てていない点、2 点目は broad root match が `Workbook.FullName` だけに寄ると nested bundle で別 sidecar を誤結合し得る点だった。どちらも CodeRabbit の actionable comment と同系統だったため採用して修正した。
  - CodeRabbit の actionable は 2 件だった。1 件目は extension test helper が snapshot を送るだけで server 反映を待っておらず flaky になり得る点、2 件目は `hasMatchedActiveWorkbookBinding()` が `normalizedFullName` しか見ておらず `worksheet-control-metadata.json` の `bundleRoot` と manifest の `bundleRoot` 不一致を見逃す点だった。加えて nit として、共有定数化、`try/finally` による snapshot reset、server test payload helper 化、`ActiveWorkbook ... .Item("...")` 粒度の追加固定が挙がった。
  - 今回は actionable 2 件に加えて、shared constant 化と `try/finally` 化も同時に取り込んだ。一方で server test の payload helper 化と `ActiveWorkbook ... .Item("...")` の追加回帰は非 blocking と判断し、この PR では見送って review log に残した。あわせて検証中に extension test harness の `dist-test` 出力先が `rootDir = "."` 変更後の配置とずれていたため、`runTest.ts` / package script / `test:host` の参照先も新しい出力構造へ揃えた。
- 指摘一覧:
  - [採用] `reviewer` / CodeRabbit: snapshot 送信 helper は server が観測した state へ反映されるまで待つべき。
    理由: global runtime state を notification で注入する以上、送信直後に assertion へ進むと test host / server 間の伝播遅延で flaky になりやすいため。
  - [採用] `reviewer` / CodeRabbit: broad root gating は `normalizedFullName` だけでなく `bundleRoot` も一致条件に含めるべき。
    理由: outer bundle の manifest と inner bundle の sidecar が同居する nested bundle で、current bundle 外の control metadata へ誤接続する余地を閉じるため。
  - [採用] CodeRabbit: extension と test の command / request 名は共有定数へ寄せるべき。
    理由: test-only infra の rename 漏れを防ぎ、notification / request / command の境界を追いやすくするため。
  - [採用] CodeRabbit: active workbook snapshot を書き換える E2E block は `try/finally` で必ず unavailable へ戻すべき。
    理由: 中途失敗時に global state が leak すると後続 fixture が連鎖的に壊れるため。
  - [非採用] CodeRabbit: workbook binding manifest / snapshot payload を test helper へ一本化する。
    理由: 有益ではあるが、今回は flaky 修正と bundleRoot 境界固定を優先し、helper 抽出だけで差分が膨らむのを避けたため。
  - [非採用] CodeRabbit: `ActiveWorkbook.Worksheets(...).OLEObjects.Item("...").Object` と `Shapes.Item("...").OLEFormat.Object` の direct / `.Item(...)` 粒度を追加固定する。
    理由: 現状の broad root gating 自体は direct 経路で十分カバーされており、`.Item(...)` 粒度の横展開は次段の unqualified broad root 検討とまとめて扱う方が整理しやすいため。
- この作業で当てはまりそうな内容（横展開候補）:
  - notification で runtime state を test 注入する helper は、「送信した payload」ではなく「server が現在観測している state」を getter で待つ形にした方が flaky を抑えやすい。
  - workbook identity と bundle-local sidecar を組み合わせる gating では、`FullName` 一致だけでなく `bundleRoot` の join も必須にしないと nested bundle の誤解決を防ぎきれない。
  - test-only command / request 名は extension test 内に直書きせず、最小の共有モジュールへ寄せて rename 追従を容易にした方が安全。
  - global state を mutate する extension E2E は `try/finally` で reset を固定し、正例追加と同時に cleanup guarantee まで入れた方が後続ケースを守りやすい。
- 実施:
  - `packages/server/src/lsp/documentService.ts` に `bundleRoot` 境界を加え、`worksheetControlMetadata.bundleRoot !== workbookBindingManifest.bundleRoot` の場合は broad root を閉じるようにした。
  - `packages/server/test/documentService.test.js` に nested bundle の manifest / sidecar mismatch 回帰を追加し、outer manifest と inner sidecar が混在しても `Value` が出ないことを固定した。
  - `packages/server/src/index.ts` と `packages/extension/src/extension.ts` に test-only request/command を追加し、`packages/extension/test/suite/index.ts` の `setActiveWorkbookIdentitySnapshot()` が server 観測 state を poll してから先へ進むようにした。
  - `packages/extension/src/testCommands.ts` を追加し、test command 名を共有化した。extension E2E の active workbook 正例 block は `try/finally` で reset するよう更新した。
  - `packages/extension/tsconfig.test.json` は `src/testCommands.ts` を test build へ含めるため `rootDir` / `include` を調整し、あわせて `packages/extension/test/runTest.ts`、`packages/extension/test/suite/index.ts`、workspace `test:host` script の `dist-test/test/...` 参照先を新しい出力構造へ揃えた。
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行して通過を確認した。
- 残課題:
  - broad root の `.Item("...")` 粒度と unqualified `Worksheets("...")` への展開可否は次段で整理する。

## 2026-03-21 PR #79 docs: active workbook identity provider 契約を追加
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、ADR `0006` と `0007` の責務分離、host raw 値 / server 正規化、`protected-view` / `unsaved` / `addin` の切り分け、次候補を snapshot transport へつなぐ流れは自然だと確認された。
  - 残留リスクとして 2 点出た。1 点目は `ActiveWorkbookIdentitySnapshot` の validation 境界が未定義だと transport 実装で payload 欠損や型不整合の扱いがぶれやすい点、2 点目は `unavailable` の reason 粒度に将来の再分類余地がある点で、前者は今回の docs に取り込んだ。
  - その結果、ADR `0007` と process 文書で server は cache 前に schema validation を行い、不正 payload は broad root を閉じたまま reject し、`active-workbook-identity.invalid-payload` を log code として持つ方針を追記した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/adr/0007-active-workbook-identity-provider-contract.md`、`docs/adr/README.md`、`docs/process/README.md`、`docs/process/active-workbook-identity-provider-contract.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`docs/process/workbook-binding-manifest-feasibility.md` を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer`: `ActiveWorkbookIdentitySnapshot` の validation 境界を transport 実装前に docs へ足した方がよい。
    理由: snapshot schema を決めても invalid payload の扱いが曖昧だと、cache と gating log の振る舞いが実装ごとにぶれやすいため。
  - [非採用] `reviewer`: `unavailable` reason を今すぐ細かく固定する。
    理由: docs 時点では `host-error` / `non-workbook-window` / `host-unreachable` 程度で十分で、運用ログを見て再分類した方が無駄が少ないため。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - runtime snapshot の契約を決める PR では、payload schema だけでなく `invalid-payload` の扱いも同時に固定した方が次段の transport 実装がやりやすい。
  - `protected-view` のように identity は見えるが resolver に使えない state は、`unavailable` へ畳まず独立 state にした方が運用ログと gating 理由を追いやすい。
  - host raw 値 / server 正規化の判断は、manifest matching helper を使う側に寄せた方が比較規則の重複を減らせる。
- 実施:
  - ADR [0007](../adr/0007-active-workbook-identity-provider-contract.md) を追加し、`ActiveWorkbookIdentitySnapshot`、`vba/activeWorkbookIdentity`、state、gating 条件を固定した。
  - [active-workbook-identity-provider-contract.md](active-workbook-identity-provider-contract.md) を追加し、official docs を根拠に state schema、transport 境界、log code、validation 方針を整理した。
  - [workbook-binding-manifest-feasibility.md](workbook-binding-manifest-feasibility.md)、[explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md)、[README.md](README.md)、[TASKS.md](../../TASKS.md) を更新し、次候補を snapshot transport の最小実装へ差し替えた。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では `vba/activeWorkbookIdentity` notification、server cache、`binding-missing` / `binding-disabled` / `invalid-payload` log を read-only で追加する。

## 2026-03-21 PR #78 docs: workbook binding manifest の方針を追加
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、manifest を sidecar から分離する判断、`Workbook.FullName` / `Path` / `IsAddin` を使う前提、次候補を `active workbook identity provider の契約整理` に置く粒度は整合していると確認された。
  - 残留リスクとして 2 点出た。1 点目は `Workbook.FullName` を「正規化して使う」と書きつつ v1 matching rule が未定義だった点、2 点目は unsaved / add-in workbook で manifest を「生成しない」のか「生成するが consumer が無効化する」のかが曖昧だった点で、どちらも妥当だったため docs に取り込んだ。
  - その結果、ADR `0006` と process 文書で `FullName` の v1 正規化を「Windows 前提で case-insensitive、`/` を `\\` へ統一、UNC は保持、`FullNameURLEncoded` は使わない」と定義し、saved かつ non-addin workbook にだけ manifest を生成し、manifest 不在は `binding disabled` とみなす責務境界を固定した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/adr/0006-workbook-binding-policy.md`、`docs/adr/README.md`、`docs/process/README.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`docs/process/workbook-binding-manifest-feasibility.md`、`docs/process/worksheet-control-metadata-sidecar-artifact.md` を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer`: `Workbook.FullName` の正規化ルールを v1 で先に固定した方がよい。
    理由: 実装時に host 側と manifest 側で比較規則がずれると、不一致の温床になるため。
  - [採用] `reviewer`: unsaved / add-in workbook で manifest をどう扱うか、generator / consumer の責務境界を明示した方がよい。
    理由: unsupported state を invalid manifest で表すのか、manifest 不在で表すのかが曖昧だと実装がぶれやすいため。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - primary key を docs で決めるときは、比較規則も同じ PR で粗く定義しておいた方が次段の host 契約が作りやすい。
  - unsupported state を扱う artifact は、「生成しない」「生成するが無効化する」のどちらかを早めに決めておかないと generator / consumer の責務がぶれやすい。
  - sidecar のような安定 schema と、path を含む volatile binding 情報は別 artifact に分けた方が diff 管理と責務分離の両面で扱いやすい。
- 実施:
  - ADR [0006](../adr/0006-workbook-binding-policy.md) を追加し、broad root 再評価の前提となる workbook binding policy を固定した。
  - [workbook-binding-manifest-feasibility.md](workbook-binding-manifest-feasibility.md) を追加し、manifest / config / workbook package mode の比較、v1 matching rule、generator / consumer 境界を整理した。
  - [explicit-sheet-name-broad-root-feasibility.md](explicit-sheet-name-broad-root-feasibility.md)、[worksheet-control-metadata-sidecar-artifact.md](worksheet-control-metadata-sidecar-artifact.md)、[README.md](README.md)、[TASKS.md](../../TASKS.md) を更新し、次候補を `active workbook identity provider の契約整理` へ差し替えた。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では extension / server / host 間で active workbook identity をどう受け渡すかを整理し、manifest の v1 matching rule を runtime 契約へ接続する。

## 2026-03-20 PR #77 docs: explicit sheet-name broad root の境界を固定
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、broad root を閉じる判断は ADR / process / `TASKS.md` で整合していると確認された。
  - 残留リスクとして、ADR `0005` の 1 行が将来形に読める点だけが挙がったため、`ThisWorkbook.Worksheets("Sheet1")` 連鎖の最小経路は実装済みと明示する表現へ修正した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/adr/0005-explicit-sheet-name-root-policy.md`、`docs/process/README.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`docs/process/shape-oleformat-object-explicit-sheet-root-feasibility.md`、`docs/process/shape-oleformat-object-promotion-feasibility.md` を ignored files として `Review skipped due to path filters` を返した。
  - そのため今回の判断根拠は `reviewer` と一次情報確認を正本にし、build / lint / test は docs-only のため実行していない。
- 指摘一覧:
  - [採用] `reviewer`: ADR `0005` の「次段の実装では...」は、すでに実装済みの内容として読めるよう表現を寄せた方が時系列が明確。
    理由: 設計判断の正本を後から読んだときに、実装済みか未実装かを誤読しにくくするため。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs-only でも long-lived な判断を固定するときは、ADR の status と時系列表現を合わせて見直した方が後から読みやすい。
  - CodeRabbit が path filters で skipped になる PR では、ignored files の一覧をレビュー要約に残しておくと、どの文書群が review 対象外だったか追いやすい。
  - broad root のような公開境界の判断は、片側の feature doc だけでなく `TASKS.md` の次候補まで同時に更新しないと、次段でスコープが戻りやすい。
- 実施:
  - `docs/process/explicit-sheet-name-broad-root-feasibility.md` を追加し、`ActiveWorkbook.Worksheets("Sheet1")` と unqualified `Worksheets("Sheet1")` を current bundle sidecar に静的接続しない理由と再評価条件を整理した。
  - `docs/adr/0005-explicit-sheet-name-root-policy.md` の status を `Accepted` に更新し、broad root 再評価条件を追記した。
  - `docs/process/README.md`、`docs/process/shape-oleformat-object-explicit-sheet-root-feasibility.md`、`docs/process/shape-oleformat-object-promotion-feasibility.md`、`TASKS.md` を更新し、次候補を workbook binding / manifest 方針整理へ差し替えた。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では current bundle と target workbook を結ぶ workbook binding / manifest 方針を整理し、broad root 再評価の前提を設計する。

## 2026-03-20 PR #76 feat: ThisWorkbook.Worksheets root の control owner 解決を追加
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは修正必須の指摘は無く、残留リスクとして `npm run test:host` をこの環境では実行していない点だけが挙がった。
  - CodeRabbit は 2 論点を出した。1 件目は `sheetCodeName` と `sheetName` が同値の fixture だと `Worksheets("...")` が誤って codeName 参照でも通ってしまう点、2 件目は未解決ルートの負例が hover のみで completion / signature help 回帰を取り逃がし得る点だった。
  - どちらも採用し、`sheetCodeName = "Sheet1"` / `sheetName = "Sheet One"` を導入したうえで、`ThisWorkbook.Worksheets("Sheet One")` のみ解決、`ThisWorkbook.Worksheets("Sheet1")` / `ThisWorkbook.Worksheets(1)` / `ActiveWorkbook.Worksheets("Sheet One")` は未解決を server / extension test で固定した。
  - extension 側は no-hover に加えて no-signature と no-completion の負例を追加し、`Shape.OLEFormat.Object` / `OLEObject.Object` の両経路で境界を揃えた。
  - review fix push は運用どおり `@coderabbitai pause` 後に実施し、最新の `coderabbitai` コメント `Reviews paused.` と `CodeRabbit: Review skipped` を確認した。local では `npm run test --workspace @vba/server`、`npm run lint`、`npm run package` は通過した一方、extension host は shared-process crash で不安定だったため、最終ゲートは PR 上の `quality-check` 成功とした。
- 指摘一覧:
  - [採用] `packages/server/test/documentService.test.js`: `sheetCodeName` / `sheetName` を分離し、`Worksheets("sheetName")` 経路を明示検証する。
    理由: `Worksheets("...")` が codeName 参照へ退行しても通る疑似陽性を除去するため。
  - [採用] `packages/extension/test/suite/index.ts`: 未解決ルート負例を hover だけでなく signature help / completion でも固定する。
    理由: 一部機能だけ誤昇格する回帰を捕捉するため。
  - [非採用] `reviewer` の残留リスク `npm run test:host` 未実行。
    理由: この環境の VS Code test host が shared-process crash で不安定で、今回の merge 判定は PR 上の `quality-check` を正本としたため。
- この作業で当てはまりそうな内容（横展開候補）:
  - `sheetCodeName` と `sheetName` を使い分ける resolver は、fixture で必ず不一致ケースを 1 本入れないと退行を検知しづらい。
  - 「未解決を維持する」境界は hover だけでは不十分で、completion / signature help も同じ入力で負例化した方が実運用の回帰を防ぎやすい。
  - `Shape.OLEFormat.Object` と `OLEObject.Object` のように helper を共有する導線は、正例と負例を両方の入口で同時に固定しないと片側だけ緩む。
  - extension host がローカルで不安定なときでも、server 単体・lint・package を先に通し、最終判定を GitHub Actions の `quality-check` に寄せる運用を要約へ残しておくと判断経緯を追いやすい。
- 実施:
  - `packages/server/test/documentService.test.js` と extension fixture / suite を更新し、`sheetName = "Sheet One"` の正例と `sheetCodeName = "Sheet1"` の負例を固定した。
  - `npm run test --workspace @vba/server`、`npm run lint`、`npm run package` を実行して通過を確認した。
  - extension host は local で shared-process crash により安定しなかったため、PR 上の `quality-check` 成功を最終確認として採用した。
- 残課題:
  - broad root の是非は未決のため、次段で `ActiveWorkbook.Worksheets("SheetName")` と unqualified `Worksheets("SheetName")` を current bundle の sidecar へ静的に結んでよいかを docs で整理する。

## 2026-03-15 PR #75 docs: explicit sheet-name root の判断を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の初回自己レビューで 2 件出た。1 件目は `sheetName` / `sheetCodeName` の使い分けと broad root の採否が長く効く判断なのに `docs/process/` だけへ置かれており、`AGENTS.md` の「重要な設計判断は `docs/adr/`」とずれている点。2 件目は、次タスクに `ThisWorkbook` 起点の workbook root identity を resolver で保持・伝播する受け入れ条件が抜けており、helper 追加だけで進める誤誘導リスクがある点だった。
  - これらは妥当だったため、ADR [0005 Explicit Sheet-Name Root Policy](../adr/0005-explicit-sheet-name-root-policy.md) を追加し、新規 process メモ [shape-oleformat-object-explicit-sheet-root-feasibility.md](../shape-oleformat-object-explicit-sheet-root-feasibility.md) では ADR を正本、process 文書を調査詳細と実装境界の補足に整理し直した。
  - あわせて `TASKS.md` の次候補 `Shape.OLEFormat.Object を ThisWorkbook.Worksheets("SheetName") root に限定接続する` に、`ThisWorkbook` 起点の workbook root identity を `Worksheets("...")` 連鎖でも保持して current bundle の sidecar へ伝播する受け入れ条件を追加した。
  - 再レビューでは `reviewer` から P0-P3 指摘なしとなり、CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/adr/0005-explicit-sheet-name-root-policy.md`、`docs/adr/README.md`、`docs/process/README.md`、`docs/process/shape-oleformat-object-explicit-sheet-root-feasibility.md`、`docs/process/shape-oleformat-object-promotion-feasibility.md`、`docs/process/worksheet-control-metadata-sidecar-artifact.md` を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer`: long-lived な判断を ADR にも残すべき。
    理由: explicit sheet-name root の join key と broad root の採否は `docs/process/` の一時メモではなく、今後の resolver 実装に効く恒久判断だったため。
  - [採用] `reviewer`: 次タスクへ workbook root identity の保持・伝播要件を入れるべき。
    理由: `sheetName + shapeName` helper 共有だけでは、generic `Worksheet` owner に降りた後の sidecar 解決要件が不足していたため。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - process メモで結論を固めたときは、`AGENTS.md` の固定ルールと照らし、長く効く判断なら同時に ADR へ昇格させた方が後続レビューでぶれにくい。
  - `TASKS.md` の次候補は「何を実装するか」だけでなく、「resolver がどの identity を保持し続ける必要があるか」まで書かないと、helper 追加だけで満たしたつもりの誤実装が起きやすい。
  - CodeRabbit が path filters で skipped になった場合でも、ignored files の一覧を残しておくと、どの種別の PR が review 対象外なのかを後から見直しやすい。
- 実施:
  - `docs/adr/0005-explicit-sheet-name-root-policy.md` を追加し、explicit sheet-name root の join key と user-facing 公開境界を ADR 化した。
  - `docs/process/shape-oleformat-object-explicit-sheet-root-feasibility.md` を追加し、`ThisWorkbook.Worksheets("Sheet1")` 先行、`ActiveWorkbook` / unqualified `Worksheets` 除外、`OLEObject.Object` / `Shape.OLEFormat.Object` での helper 共通化余地を整理した。
  - `docs/adr/README.md`、`docs/process/README.md`、`docs/process/shape-oleformat-object-promotion-feasibility.md`、`docs/process/worksheet-control-metadata-sidecar-artifact.md`、`TASKS.md` を更新した。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では `ThisWorkbook.Worksheets("Sheet1").Shapes("CheckBox1").OLEFormat.Object` と `.Item("CheckBox1")` だけを sidecar へ結び、workbook root identity を保持したまま `sheetName + shapeName` lookup を共有 helper へ寄せる。

## 2026-03-14 PR #66 docs: Worksheet / Chart control metadata source PoC を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、docs-only としてスコープ、一次情報の根拠、`TASKS.md` の更新内容は整合していると確認された。
  - 非 blocking の残留リスクとして、worksheet 側で `controls` / `oleObjects` / drawing の `shapeId` 突合を PoC の必須検証へ落としておくこと、初回対象形式を `.xlsm` / `.xlam` に固定すること、chart sheet 保留の解除条件を次候補で明示することが挙がった。
  - これらは妥当だったため、PR 前に `docs/process/worksheet-chart-control-metadata-source-poc.md` と `TASKS.md` へ反映した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/process/README.md`、`docs/process/worksheet-chart-control-metadata-source-poc.md` を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer` の残留リスクを受け、`worksheet-chart-control-metadata-source-poc.md` に `controls/control@shapeId`、`oleObject@shapeId`、drawing `xdr:cNvPr@id` の突合を PoC の必須検証として追記した。
  - [採用] `reviewer` の残留リスクを受け、初回 PoC 対象を `.xlsm` / `.xlam` へ寄せ、`.xlsb` は別判断に切り分けることを明記した。
  - [採用] `reviewer` の残留リスクを受け、`TASKS.md` に chart sheet 保留解除条件を明文化する旨を追記した。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs-only の PoC メモでも、次段の実装で必須になる突合条件や fixture 条件を先に書いておくと、後続 PR で「何を証明すれば成立か」がぶれにくい。
  - workbook package を source of truth、sidecar artifact を delivery format と分けて整理すると、既存の loose file 主軸を崩さずに拡張しやすい。
  - worksheet と chart sheet の確度が非対称なときは、同じタスク名でも先行対象と保留条件を分けて書いた方が、未証明部分を既成事実化せずに済む。
  - CodeRabbit が path filters で skipped になった場合でも、ignored files 一覧をログへ残しておくと、後から「なぜレビューされなかったか」を追いやすい。
- 実施:
  - `docs/process/worksheet-chart-control-metadata-source-poc.md` を追加し、workbook package を起点にした worksheet 先行 PoC と sidecar 方針を整理した。
  - `docs/process/README.md` に導線を追加し、`TASKS.md` を更新して本タスク完了と次候補 `Worksheet workbook package control metadata probe の最小実装` を反映した。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では workbook package から worksheet control metadata を抜く最小 probe を追加し、`shape name != code name` と `shapeId` 突合成立を fixture で確認する。

## 2026-03-14 PR #65 docs: Worksheet / Chart control identity source を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped due to path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、新規メモの結論、`TASKS.md` の stale な次候補差し替え、既存 docs からの導線は妥当と確認された。
  - 非 blocking の残留リスクとして、「静的入力だけでは inventory を復元できない」という前提は将来 `.frx` や抽出 artifact の取り込み経路が増えたときに古くなり得るため、PoC 完了時に判断メモを更新する前提を明記した。
  - CodeRabbit は docs-only 差分を path filters 対象として扱い、`TASKS.md`、`docs/process/README.md`、`docs/process/mslearn-signature-regeneration.md`、`docs/process/worksheet-chart-control-identity-feasibility.md` を ignored files として `Review skipped due to path filters` を返した。
- 指摘一覧:
  - [採用] `reviewer` の残留リスクメモを受け、`docs/process/worksheet-chart-control-identity-feasibility.md` に「PoC 成立時は本メモと `TASKS.md` の前提を更新する」旨を追記した。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped due to path filters`。
- この作業で当てはまりそうな内容（横展開候補）:
  - `.Object` 後段型付けと control code name 支援のように見えて別機能に見える論点でも、必要 metadata が同一なら 1 本の判断メモへ束ねた方が次段の PoC を設計しやすい。
  - stale な `TASKS.md` の次候補は、その場で新しい正本 docs と 1 対 1 に対応する粒度へ直した方が、後続 PR で再度スコープを迷いにくい。
  - CodeRabbit の path filters で docs-only PR が skipped になる場合でも、ignored files の一覧を要約へ残しておくと「なぜ skipped だったか」を後から追いやすい。
- 実施:
  - `docs/process/worksheet-chart-control-identity-feasibility.md` を追加し、shape name / code name / ProgID / 現行静的入力の不足を 1 か所で整理した。
  - `docs/process/README.md` と `docs/process/mslearn-signature-regeneration.md` に導線を追加し、`TASKS.md` の次候補を `Worksheet / Chart control metadata source の PoC` へ更新した。
  - docs-only のためテストは実行していない。
- 残課題:
  - 次段では workbook package や抽出 artifact を含む metadata source の PoC を行い、`shape name` / `code name` / `ProgID` のどこまで復元できるかを比較する。

## 2026-03-14 PR #64 feat: OLEObjects.Item の型解決を追加
- レビュー状況: `COMMENTED（対応済み・Reviews paused 予定）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P3 指摘は無く、低優先度で `OLEObjects.Item(Array(...))` や括弧を含む文字列 selector のような境界は未固定だが、今回の最小範囲では十分という確認に留まった。
  - CodeRabbit は `scripts/generate-mslearn-vba-reference.mjs` と `scripts/test/mslearnReferenceAudit.test.mjs` に 2 件の nit を出した。member override 適用時に `name` まで上書きして canonical casing を壊し得る点と、監査テストの `itemOverride` 探索条件が `owner + member` のみで将来衝突し得る点で、どちらも妥当だったため採用した。
  - そのため review fix として、override 適用では selector と patch を分離して `name` を除外し、監査テストでは `ownerName === "OLEObjects"` かつ `sectionName === "Methods"` の設定から `Item` を引く形へ絞り、`typeName === "OLEObjects"` も明示検証した。
  - CodeRabbit の最新レビューは `Review completed` で、actionable comment は上記 2 件のみだった。`TASKS.md` は path filters により review 対象外だった。
- 指摘一覧:
  - [採用] `scripts/generate-mslearn-vba-reference.mjs` の `applySupplementalOwnerMemberOverrides` で、`memberOverride.name` を検索キー専用とし、`Object.assign` には `name` を含めないよう修正した。
  - [採用] `scripts/test/mslearnReferenceAudit.test.mjs` の `OLEObjects.Item` 監査で、`supplementalOwnerMemberOverrides` から `ownerName + sectionName + memberName` で一意に探索し、`typeName` も直接検証するよう修正した。
  - [非採用] `reviewer` の低優先メモだった `OLEObjects.Item(Array(...))` や `Item("A(1)")` の追加回帰。
    理由: 今回の PR 範囲は `Item(1)` / `Item("CheckBox1")` / 単純式 / function call までの owner 正規化に限定しており、追加境界は次段で十分なため。
- この作業で当てはまりそうな内容（横展開候補）:
  - 生成時の override は「探索キー」と「上書き payload」を同じ object に載せると、`name` や `title` の canonical 値を静かに壊し得る。member / section / owner override 全般で patch 前に識別子を剥がす方が安全。
  - 監査テストで設定配列自体を探索するときは、`owner + section + member` まで絞らないと、将来の同名 member 追加で false positive が出やすい。
  - `Collection.Item` の型補完は runtime 側だけでなく参照 JSON の `typeName` 正規化も絡むため、生成器の patch 処理を触った PR では script 監査を必ず独立に回した方がよい。
- 実施:
  - `OLEObjects.Item` 用の member override を維持したまま、override 適用の安全化を実装した。
  - `npm run generate:reference-data`、`npm run test:scripts`、`npm run lint` を実行して通過を確認した。
  - review fix push 前のため `@coderabbitai pause` を付けてから push し、再レビュー待ちは行わない運用で進める。
- 残課題:
  - `OLEObject.Object` 後段型付けと `OLEObjects.Item(Array(...))` などの grouped selector 境界は次候補として維持する。

## 2026-03-14 PR #63 feat: Worksheet / Chart OLEObjects root を追加
- レビュー状況: `SUCCESS（CodeRabbit: No actionable comments）`
- 要約:
  - `reviewer` の事前自己レビューでは P0-P2 指摘は無く、低優先で `OLEObjects.Item(1)` は未対応のままである点と、extension E2E に `.Object.` の保守動作を足すとより堅い、という 2 点だけが出た。
  - そのため PR 前に extension E2E へ `.Object.` で `Activate` を出さない負例を追加し、`OLEObjects.Item(1)` は参照 JSON 側の `typeName` 欠落により今回は範囲外と明示した。
  - CodeRabbit は `packages/core/src/reference/builtinReference.ts`、server / extension の fixture / test を処理対象にし、最新コメントは `No actionable comments were generated in the recent review.` だった。`TASKS.md` は path filters により review 対象外だった。
- 指摘一覧:
  - [採用] `reviewer` 指摘に基づき、`packages/extension/test/suite/index.ts` へ `Sheet1.OLEObjects(1).Object.` の negative completion 回帰を追加した。
  - [非採用] `OLEObjects.Item(1)` の型解決対応。
    理由: 参照 JSON 上の `OLEObjects.Item` が `typeName` を持たず、今回の最小実装範囲は `OLEObjects(Index)` root に限定しているため。次候補として残す。
  - [非採用] CodeRabbit actionable comment なし。`Docstring Coverage` warning は現行品質ゲート対象外のため対応しない。
- この作業で当てはまりそうな内容（横展開候補）:
  - collection root の最小実装では、肯定系の completion / hover / signature help だけでなく、`.Object.` のような「そこで止める」負例を extension E2E でも 1 件入れておくと回帰を見つけやすい。
  - `Collection.Item` の `typeName` 欠落は root 側 indexed access 正規化だけでは埋まらないため、次段で `Item` を触るなら参照データ補完か runtime mapping のどちらを正本にするか先に決めた方がよい。
  - CodeRabbit の path filters で `TASKS.md` が外れる場合でも、実装差分の core / server / extension が十分に見られていれば docs-only 追記は別コミットで切り分けやすい。
- 実施:
  - `INDEXED_COLLECTION_OWNER_TYPES` に `oleobjects -> OLEObject` を追加し、`Worksheet` / `Chart` root の indexed `OLEObjects` 解決を有効化した。
  - server / extension に `OleObjectBuiltIn` fixture と専用回帰を追加し、literal / string / simple expression selector と function-based selector の保守動作を固定した。
  - `npm test`、`npm run lint`、`npm run package` を通過させた。
- 残課題:
  - `OLEObjects.Item(1)` の型解決、`OLEObject.Object` 後段型付け、control code name / `Shapes` root は次候補として維持する。

## 2026-03-14 PR #62 docs: Worksheet / Chart control entry point 方針を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped）`
- 要約:
  - `reviewer` の事前自己レビューでは、P0/P1 は無く、`TASKS.md` の次候補文言で collection surface と indexed access の扱いが衝突している点と、「完了」が調査完了なのか実装完了なのか読み取りづらい点の 2 件だけが出た。
  - そのため PR 前に `TASKS.md` を修正し、`Worksheet.OLEObjects` / `Chart.OLEObjects` は index 省略時に collection surface を維持し、`OLEObjects(Index)` / `.Item(Index)` だけを `OLEObject` へ落とすこと、今回完了したのは調査と方針整理までで実装は未着手であることを明示した。
  - CodeRabbit は docs-only 差分を `Review skipped` とし、actionable comment は出なかった。最新の `coderabbitai` コメントは無く、check の `CodeRabbit` 行で `Review skipped` を確認した。
- 指摘一覧:
  - [採用] `TASKS.md` の次候補で `Worksheet.OLEObjects.` / `Chart.OLEObjects.` は collection surface 維持、`OLEObjects(Index)` / `.Item(Index)` だけを `OLEObject` へ落とすと明記した。
  - [採用] `TASKS.md` の完了項目へ「今回は調査と方針整理までで実装は未着手」と追記し、進捗誤認を避けるようにした。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped`。
- この作業で当てはまりそうな内容（横展開候補）:
  - collection と indexed access を同じ 1 行で説明すると、`.` 後補完と `(Index)` 後補完の責務が混ざりやすいので、`TASKS.md` でも明示的に分けた方がよい。
  - docs-only で「完了」と書くときは、調査完了か実装完了かを一言添えないと、次候補の粒度がぶれやすい。
  - CodeRabbit がコメントを残さず `Review skipped` だけ返すケースでは、最新コメントが `null` でも `gh pr checks` の `CodeRabbit` 行を確認した事実を要約に残す方が後から追いやすい。
- 実施:
  - `docs/process/worksheet-chart-control-entrypoint-feasibility.md` を追加し、`OLEObjects` を first prototype、`.Object` / control code name / `Shapes` を後続とする判断を整理した。
  - `docs/process/README.md` と `docs/process/mslearn-signature-regeneration.md` に新規文書への導線を追加した。
  - `TASKS.md` に今回の完了項目と次候補 `Worksheet / Chart OLEObjects root の最小プロトタイプ` を反映し、`reviewer` 指摘を取り込んだ。
- 残課題:
  - 次段では `Worksheet.OLEObjects` / `Chart.OLEObjects` / `OLEObjects.Item` の owner 正規化と `OLEObject` surface の最小公開を code / test で固定する必要がある。

## 2026-03-14 PR #61 docs: Worksheet / Chart control collection 方針を整理
- レビュー状況: `SUCCESS（CodeRabbit: Review skipped）`
- 要約:
  - `reviewer` の事前自己レビューでは、P0/P1 は無く、次候補タスクが `OLEObjects / control name` に寄りすぎて `Shapes` が落ちている点と、完了条件が弱く同論点を再燃させやすい点の 2 件だけが出た。
  - そのため PR 前に `TASKS.md` を修正し、次候補を `Worksheet / Chart OLEObjects / Shapes / control name 導線整理` へ更新し、対象シナリオ表、owner 判定ルール、`.Object` 後段型付け方針、`Shapes` を後回しにする場合の理由を完了条件へ追加した。
  - CodeRabbit は docs-only 差分を `Review skipped` とし、actionable comment は出なかった。最新の `coderabbitai` コメントは無く、check の `CodeRabbit` 行で `Review skipped` を確認した。
- 指摘一覧:
  - [採用] `TASKS.md` の次候補へ `Shapes` 導線を明示し、方針文の「正本道線」とスコープをそろえた。
  - [採用] `TASKS.md` の次候補へ完了条件を追加し、同論点の再検討で停滞しにくい形へ補強した。
  - [非採用] CodeRabbit actionable comment なし。docs-only 差分のため `Review skipped`。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs タスクでも、結論で挙げた正本道線と `TASKS.md` の次候補スコープがずれると、次段で優先順位が崩れやすい。
  - 「次に何を整理するか」だけでは再検討に戻りやすいため、docs-only PR でも次候補の完了条件を最低限書いておく方がよい。
  - CodeRabbit がコメントを付けずに check だけ `Review skipped` で終わるケースがあるため、review 一覧ではなく `gh pr checks` の結果も合わせて見た方が確実。
- 実施:
  - `docs/process/worksheet-chart-control-collection-feasibility.md` を追加し、Office VBA と interop の一次情報を基に `Worksheet` / `Chart` への横展開方針を整理した。
  - `docs/process/README.md` と `docs/process/mslearn-signature-regeneration.md` に新規文書への導線を追加した。
  - `TASKS.md` に完了項目と次候補を反映し、`reviewer` 指摘を取り込んだ。
- 残課題:
  - 次段では `Worksheet` / `Chart` の `OLEObjects` / `Shapes` / control code name 導線を比較し、どこから user-facing 解決を始めるかを決める必要がある。

## 2026-03-14 PR #60 DialogSheet control collection の single-selector 正規化
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは、literal 判定の厳密性、`Item` 型メタデータ、数値 literal 境界、生成物監査の 4 論点が指摘され、P1 の literal 判定は既存 regex で満たしていることを確認したうえで、数値 literal テスト追加、`Item` の運用コメント追加、監査強化を先に取り込んでから PR 化した。
  - CodeRabbit では 4 件の nit が出た。`scripts/test` が実装設定をそのまま期待値に使っていた点、core 側の collection -> item 対応表と supplemental config の二重管理、`.Item("...")` 分岐の不足、extension 側 E2E で非 10 進 / 型付き numeric literal を踏んでいない点で、いずれも妥当だったため採用した。
  - review fix の途中で `Buttons(1#).Select(` が `Buttons(Index)` の signature help に落ちる回帰が見つかり、`getCallContext` の `#` 判定を「date literal の開始時だけ skip する」条件へ修正して解消した。
- 指摘一覧:
  - [採用] `scripts/test/mslearnReferenceAudit.test.mjs` の DialogSheet control collection 監査で、`supplementalReferenceConfig.mjs` を期待値として再利用するのをやめ、固定の期待 surface に置き換えた。
  - [採用] `packages/server/test/documentService.test.js` に `DialogSheet` control collection owner と supplemental config の整合テストを追加し、collection / single / literal の owner 解決がずれないことを固定した。
  - [採用] `packages/server/test/documentService.test.js` と `packages/extension/test/suite/index.ts` に `.Item("Button 1")` / `.Item("Check 1")` / `.Item("Option 1")` の completion / signature help 回帰を追加した。
  - [採用] `packages/extension/test/suite/index.ts` に `&H1` / `&O7` / `1#` / `1E+2` の numeric literal 経由 completion / signature help 回帰を追加した。
  - [採用] `packages/server/src/lsp/documentService.ts` の call context 解析で、`1#` の型サフィックスを date literal と誤認しないよう `isDateLiteralStart` を追加した。
- この作業で当てはまりそうな内容（横展開候補）:
  - 生成設定をそのままテスト期待値に流用すると、allow list や owner 名の破損をテストが検知できなくなるため、監査系は固定期待値か独立 fixture を持たせた方がよい。
  - built-in owner 解決の表と supplemental config のように、実行時マップと生成時マップが分かれる箇所は、どちらかを正本に寄せるか、少なくとも整合テストを常設した方が安全。
  - selector 正規化は `Buttons(1)` と `Buttons("x")` だけでなく、`.Item("x")`、hex/octal、型サフィックス、指数表記まで E2E で押さえないと、解析系の走査バグが残りやすい。
  - `#` は VBA で date literal の区切りにも型サフィックスにも使われるため、字句走査で一律 special-case にすると call context や signature help が壊れやすい。
- 実施:
  - core / server / extension / scripts の差分に対して `reviewer` を実施し、前述 4 論点を PR 前に反映した。
  - CodeRabbit 指摘対応として、監査期待値の独立化、owner 解決整合テスト、`.Item("...")` と numeric literal の E2E 追加、`1#` 誤判定修正を実施した。
  - `npm run lint`、`npm run test:scripts`、`npm test`、`npm run package` を再実行して通過を確認した。
- 残課題:
  - review fix push は `@coderabbitai pause` 後に行い、再レビュー待ちはせずにマージする。

## 2026-03-14 PR #59 DialogFrame 補助参照の最小プロトタイプ
- レビュー状況: `COMMENTED`
- 要約:
  - `reviewer` の事前自己レビューでは、重大指摘は無く、`DialogSheet.DialogFrame -> DialogFrame` の type chain、grouped selector 抑止、`TASKS.md` の整合は問題なしと確認された。
  - 低優先度の指摘として、`DialogSheets.Item(1).DialogFrame` / `DialogSheets("Dialog1").DialogFrame` の回帰と、`DialogFrame.Caption(` / `Text(` に signature help を出さない負例が不足していたため、PR 前に追加で固定した。
  - CodeRabbit では、negative signature help helper が `SignatureHelp { signatures: [] }` を誤検知し得る点と、supplemental property の `typeName` 欠落時に生成を継続してしまう点の 2 件が指摘され、どちらも採用した。
- 指摘一覧:
  - [採用] `DialogSheets("Dialog1").DialogFrame.` の completion 回帰を server / extension テストへ追加。
  - [採用] `DialogSheets.Item(1).DialogFrame.Select(` の signature help 回帰を server / extension テストへ追加。
  - [採用] `DialogSheets(1).DialogFrame.Caption(` と `Application.DialogSheets(1).DialogFrame.Text(` で signature help を返さない負例を server / extension テストへ追加。
  - [採用] `waitForNoSignatureHelp` を `signatureHelp?.signatures.length > 0` 判定へ変更し、空配列を「signature help なし」として扱うよう修正。
  - [採用] supplemental property 解析で `returnType` を取得できない場合は fail-fast とし、`buildSupplementalInteropOwner` 側でも `typeName` 欠落を例外化。
- この作業で当てはまりそうな内容（横展開候補）:
  - 補助 owner を追加するときは、indexed selector だけでなく `Item(...)` と named selector の chain も同時に固定しないと、既存 override の退行を見逃しやすい。
  - property を型付きで取り込む生成ロジックでは、正例の hover/completion だけでなく `PropertyName(` に signature help を出さない負例も回帰テストへ入れた方が安全。
  - negative helper は `undefined` だけでなく「空だが存在するレスポンス」も吸収できるようにしておかないと、VS Code API の返し方の差分で false negative が出る。
  - supplemental property は `summary` だけ拾えても chain 解決には不十分なので、`typeName` が無い時点で生成失敗に倒した方が reference JSON の破損を早く検知できる。
- 実施:
  - `DialogFrame` owner の allow list と `DialogSheet.DialogFrame` property を生成スクリプトへ追加し、参照 JSON を再生成。
  - script / server / extension テストを拡張し、`DialogFrame` の completion / hover / signature help / semantic token と grouped selector 抑止を確認。
  - `TASKS.md` を更新し、本タスクを完了へ移し、次候補を control collection の single-selector 正規化へ更新。
  - CodeRabbit 指摘を受け、extension の negative signature help helper と supplemental property metadata 抽出の fail-fast を修正した。
- 残課題:
  - review fix push 後に `generate/test/package` と PR checks を再確認してからマージする。

## 2026-03-09 PR #39 組み込み署名データ第4弾を追加
- レビュー状況: `COMMENTED`
- 要約:
  - `WorksheetFunction.Or` / `Xor` の可変引数メタデータ（`Arg2+`）が不足していた。
- 指摘一覧:
  - [採用] `Or` の `Arg2..Arg30` に `dataType` / `description` / `isRequired` を補完。
  - [採用] `Xor` の `Arg2..Arg29` に同様の補完。
- この作業で当てはまりそうな内容（横展開候補）:
  - variadic 署名を持つ他メソッドでも、先頭引数のみ定義されるパターンのメタデータ欠落が起こり得る。
  - 省略記号の表記ゆれ（`...` / `…`）を共通処理で吸収しておくと再発しにくい。
- 実施:
  - 生成スクリプトに連番引数の不足メタデータ補完を追加。
  - `Or` / `Xor` の引数ドキュメント検証を server / extension テストへ追加。
- 残課題:
  - 追加対象メソッド拡張時に同ルールが効くか、回帰確認を継続する。

## 2026-03-09 PR #40 組み込み署名データ第5弾を追加
- レビュー状況: `COMMENTED`
- 要約:
  - extension テストの位置依存（固定オフセット）と、生成 JSON の `generatedAt` 差分ノイズが指摘された。
- 指摘一覧:
  - [採用] `BuiltInMemberSignature` テストの新規追加領域を、トークン検索ヘルパーで `Position` 解決する形へ変更。
  - [採用] `mslearn-vba-reference.json` から `generatedAt` を除外。
- この作業で当てはまりそうな内容（横展開候補）:
  - fixture 追記で位置がずれるテストは、文字列検索ヘルパーへ寄せると保守性が上がる。
  - 生成物コミット運用では、実行に不要な時刻系メタデータは原則除外したほうがレビュー効率が高い。
- 実施:
  - `findPositionAfterToken` を追加し、該当テストで固定オフセットを置換。
  - 生成スクリプトから `generatedAt` 出力を削除し、参照 JSON を再生成。
- 残課題:
  - 他テストにも固定オフセットが残っているため、必要に応じて段階的にヘルパー化する。

## 2026-03-09 PR #41 docs: PR前レビューの既定エージェントを reviewer に統一
- レビュー状況: `SKIPPED`
- 要約:
  - CodeRabbit は path filters により docs-only 差分をレビュー対象外とし、指摘は出なかった。
- 指摘一覧:
  - [非採用] 指摘なし。`AGENTS.md` / `docs/process/` / `TASKS.md` はすべて path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs-only PR は CodeRabbit の path filters でスキップされることがあるため、事前のサブエージェントレビューと人手確認の重要度が上がる。
  - 運用ルール変更時は、正本ドキュメントだけでなく `AGENTS.md` と `TASKS.md` の整合も同時に見ると漏れが減る。
- 実施:
  - `reviewer` を PR 前レビューの既定エージェントに統一する文言を `AGENTS.md` / `docs/process/` / `TASKS.md` に反映。
  - CodeRabbit レビュー要約ログ運用を追加し、過去 PR のレビュー要約も記録開始。
- 残課題:
  - docs 系ファイルも CodeRabbit 対象に含めるかは `.coderabbit.yaml` の方針として別途判断が必要。

## 2026-03-10 PR #42 feat: 組み込み署名データ第6弾を追加
- レビュー状況: `COMMENTED`
- 要約:
  - `Transpose` の fixture が配列を `Debug.Print` へ直接渡しており、実行可能性に難があった。
  - `Choose` の required 引数確認が `Arg2` のみで、末尾引数までの回帰監視が不足していた。
  - `Choose` の `Arg3..Arg30` を optional とみなすべきではないかという指摘もあったが、現行 Microsoft Learn 記述との整合を優先して今回は保留した。
- 指摘一覧:
  - [採用] `WorksheetFunction.Transpose` の結果を Variant で受け、配列サイズだけを `Debug.Print` する形へ fixture を修正。
  - [採用] `Choose` の required 検証を末尾引数 (`Arg30`) まで追加し、回帰検知を強化。
  - [非採用] `Choose` の `Arg3..Arg30` を optional へ変更する提案。
    理由: 現行 Microsoft Learn の VBA API 記述では `Arg2 - Arg30` が required 扱いであり、このリポジトリではまず Microsoft Learn 由来データとの一致を優先するため。
- この作業で当てはまりそうな内容（横展開候補）:
  - 署名 fixture に実行結果が配列になる呼び出しを置く場合は、直接 `Debug.Print` せず、型や境界値の出力へ寄せると誤解が減る。
  - 可変長引数テストは先頭だけでなく末尾側も押さえておくと、途中の補完ロジック変更に強くなる。
  - Microsoft Learn と実ランタイムの差が疑われる項目は、生成データの出典優先順位を事前に明文化しておくと判断がぶれにくい。
- 実施:
  - extension / server の `BuiltInMemberSignature` 系ケースを修正し、`Transpose` を安全な fixture へ変更。
  - extension / server テストに `Choose` 最終引数の required 確認を追加。
- 残課題:
  - `Choose` の required / optional 判定を Microsoft Learn 準拠のまま維持するか、Excel 実動作優先へ寄せるかは別途方針整理が必要。

## 2026-03-10 PR #43 組み込みメンバー署名データ第7弾: Address 系シグネチャを追加
- レビュー状況: `SUCCESS`
- 要約:
  - CodeRabbit から妥当な修正指摘は出ず、`No actionable comments were generated` で完了した。
  - `Docstring Coverage` 警告は出たが、このリポジトリの現行品質ゲート対象ではなく、今回差分の妥当性を崩す指摘ではないため対応対象外とした。
- 指摘一覧:
  - [非採用] 指摘なし。CodeRabbit の walkthrough と pre-merge check 警告のみで、コード修正を要するコメントは無かった。
- この作業で当てはまりそうな内容（横展開候補）:
  - allow list で property ページの署名を取り込む場合、enrich 対象が `Methods` に固定されていないかを先に確認すると手戻りが減る。
  - `Return value` 節が無い Microsoft Learn ページでは、summary から戻り値を補完できるかを検討すると署名ラベルの欠損を防ぎやすい。
  - `ActiveCell` や `Cells` のような built-in root は、実際の返却型を `typeName` で持たせるとメンバー補完と署名解決の両方に効く。
- 実施:
  - `Range.Address` / `Range.AddressLocal` の署名を参照 JSON へ追加。
  - `ActiveCell.Address` / `Cells.AddressLocal` の signature help と shadowing 抑止を server / extension テストで確認。
  - PR 前 `reviewer` では重大度付き指摘なし、追加で `ActiveCell.Address` の shadowing 抑止テストを補強した。
- 残課題:
  - `XLookup` / `XMATCH` は現行 Microsoft Learn では未掲載のため、次回の参照再生成時に再確認する。

## 2026-03-10 PR #44 fix: 既存署名メタデータ監査と Max/Min 補完
- レビュー状況: `COMMENTED`
- 要約:
  - `Max` / `Min` 補完自体への重大指摘はなく、可読性と保守性に関する nit が 3 件出た。
  - 監査テストの variadic 判定で `label` に依存していたため、構造化データだけを見る形へ寄せた。
- 指摘一覧:
  - [採用] `scripts/generate-mslearn-vba-reference.mjs` の variadic tail optional 正規化に短い意図コメントを追加。
  - [採用] `packages/extension/test/suite/index.ts` の `Max` / `Min` 署名検証を helper 化して重複を削減。
  - [採用] `scripts/test/mslearnReferenceAudit.test.mjs` の variadic 監査条件から `signature.label.includes(\"...\")` を外し、`parameters` / `description` ベースへ寄せた。
  - [非採用] `Docstring Coverage` 警告。
    理由: このリポジトリの現行品質ゲート対象外であり、今回差分の正当性を崩すものではないため。
- この作業で当てはまりそうな内容（横展開候補）:
  - 監査ロジックは表示用ラベルより、`parameters` や `description` のような構造化データを優先したほうがフォーマット変更に強い。
  - built-in signature 系テストは、関数ごとの差分が薄い場合 helper 化しておくと後続メソッド追加時のレビュー負荷を下げやすい。
  - 生成スクリプトの allow-list と監査対象は同じ設定ファイルを参照させると更新漏れを減らせる。
- 実施:
  - `WorksheetFunction.Max` / `Min` の `Arg1` metadata 欠落と `Arg30` required 誤判定を修正。
  - `scripts/lib/referenceSignatureConfig.mjs` を追加し、allow-list を生成スクリプトと監査テストで共有化。
  - CodeRabbit 指摘に合わせて extension テスト helper 化、variadic 判定の構造化、意図コメント追加を反映。
- 残課題:
  - `XLookup` / `XMATCH` は引き続き現行 Microsoft Learn に未掲載のため、再生成時にテスト failure を入口として更新要否を判断する。

## 2026-03-10 PR #45 docs: レビュー重複指摘時の判断基準を明確化
- レビュー状況: `SKIPPED`
- 要約:
  - CodeRabbit は path filters により docs-only 差分をレビュー対象外とし、指摘は出なかった。
  - PR 前の `reviewer` 自己レビューでも追加指摘はなく、正本ドキュメントへの集約と判断基準の明確化だけを反映した。
- 指摘一覧:
  - [非採用] 指摘なし。`AGENTS.md`、`TASKS.md`、`docs/process/coderabbit-review.md`、`docs/process/sub-agent-escalation.md` は CodeRabbit の path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - 運用ルール変更は正本ドキュメントに判断基準を集約し、参照側は短い導線に留めると重複と矛盾を減らせる。
  - `required` / `optional` のような挙動判断は、「出典準拠」か「運用優先」かではなく、互換性、既存テスト、誤案内防止の順で比較基準を固定するとレビュー判断がぶれにくい。
  - docs-only PR は CodeRabbit が継続的に skipped になるため、`reviewer` の事前確認内容を要約ログへ残しておく価値が高い。
- 実施:
  - `docs/process/coderabbit-review.md` に、自己レビューと CodeRabbit の重複指摘を原則修正とする方針、および `required` / `optional` 判断の基準を追加。
  - `docs/process/sub-agent-escalation.md` と `AGENTS.md` は正本参照の形へ整理し、重複記載を抑制。
  - `TASKS.md` に今回の運用更新を反映。
- 残課題:
  - docs 系ファイルを CodeRabbit の対象へ含めるかどうかは、引き続き `.coderabbit.yaml` 側の方針として検討余地がある。

## 2026-03-11 PR #46 docs: Microsoft Learn 署名再生成手順を追加
- レビュー状況: `SUCCESS`
- 要約:
  - CodeRabbit は `scripts/test/mslearnReferenceAudit.test.mjs` のみを処理対象とし、`No actionable comments were generated` で完了した。
  - docs 本体は path filters で review 対象外だったが、監視テストの失敗メッセージ強化については問題なしと判断された。
- 指摘一覧:
  - [非採用] 指摘なし。`scripts/test/mslearnReferenceAudit.test.mjs` に対する actionable comment は出なかった。
- この作業で当てはまりそうな内容（横展開候補）:
  - path filters で docs が除外される場合でも、レビュー対象に残る test や script から正本ドキュメントへ誘導しておくと運用の実効性が上がる。
  - 監視テストは単に「未掲載」を示すだけでなく、追加時の次アクションまでメッセージに含めると手戻りが減る。
  - `WorksheetFunction` 以外の監視対象を増やす場合も、owner ごとに同じ手順書へ集約しておくと update point の分散を防ぎやすい。
- 実施:
  - `docs/process/mslearn-signature-regeneration.md` を新規追加し、allow list、再生成、built-in index、server / extension テスト、レビュー記録までの更新箇所を整理。
  - `scripts/test/mslearnReferenceAudit.test.mjs` の監視失敗メッセージから手順書へ誘導する文言を追加。
  - `AGENTS.md` と `TASKS.md` に導線と完了記録を反映。
- 残課題:
  - `WorksheetFunction` 以外の監視対象を owner 単位で共通化するかは、次候補として検討を続ける。

## 2026-03-11 PR #47 test: Microsoft Learn 未掲載監視を watch list 化
- レビュー状況: `COMMENTED`
- 要約:
  - CodeRabbit は `scripts/lib/referenceSignatureConfig.mjs` と `scripts/test/mslearnReferenceAudit.test.mjs` をレビューし、watch list 正規化の重複と失敗メッセージの正本分離について nitpick 2 件を出した。
  - どちらも妥当だったため採用し、`@coderabbitai pause` 後に軽微修正だけを push した。
- 指摘一覧:
  - [採用] `getOwnerMemberNames` のインライン小文字化を `normalizeMemberName` helper に統一し、正規化ルールの二重管理を解消。
  - [採用] `buildMissingMemberGuidance` の詳細手順を削り、`docs/process/mslearn-signature-regeneration.md` を正本として参照する文言へ整理。
- この作業で当てはまりそうな内容（横展開候補）:
  - 監視系テストで大文字小文字を正規化する処理は、存在確認、重複検知、メッセージ生成の各所で helper に寄せたほうがずれにくい。
  - テスト失敗メッセージに手順の詳細を書きすぎると docs と二重管理になるため、手順の正本は docs に寄せてテスト側は短い誘導に留めるのがよい。
  - CodeRabbit nitpick が自己レビューの観点と噛み合う領域では、軽微修正でも横展開しやすい helper 化や正本参照化を優先すると再発を減らせる。
- 実施:
  - `scripts/lib/referenceSignatureConfig.mjs` に `signatureMissingMemberWatchList` を追加し、未掲載監視を owner 単位設定へ移行。
  - `scripts/test/mslearnReferenceAudit.test.mjs` を watch list ベースの監視、allow list 重複検知、watch list 内の case-insensitive 重複検知へ更新。
  - `docs/process/mslearn-signature-regeneration.md` と `TASKS.md` を watch list -> allow list の移行手順に合わせて更新。
  - CodeRabbit 指摘対応として `normalizeMemberName` の再利用と失敗メッセージの正本参照化を追加。
- 残課題:
  - 現在の watch list 実体は `WorksheetFunction` のみなので、次段階で監視対象 owner の候補整理を続ける。

## 2026-03-11 PR #48 test: Range の未掲載監視を追加
- レビュー状況: `SUCCESS`
- 要約:
  - CodeRabbit は `scripts/lib/referenceSignatureConfig.mjs` のみを処理対象とし、`No actionable comments were generated` で完了した。
  - `docs/process/mslearn-signature-regeneration.md` と `TASKS.md` は path filters で review 対象外だったが、watch list の `Range` 追加自体には指摘が出なかった。
- 指摘一覧:
  - [非採用] 指摘なし。`scripts/lib/referenceSignatureConfig.mjs` に対する actionable comment は出なかった。
- この作業で当てはまりそうな内容（横展開候補）:
  - watch list の owner を増やすときは、まず Microsoft Learn に個別ページがあり、かつローカル参照 JSON に未掲載のメンバーへ絞ると監視の意味が明確になる。
  - docs が path filters で除外される運用では、CodeRabbit 指摘の有無にかかわらず、なぜその owner を監視するのかを要約ログへ残しておくと次回判断が速い。
  - 動的配列のような近年の Excel 機能は `WorksheetFunction` とは別 owner に散らばるため、owner 単位 watch list を継続的に広げる価値が高い。
- 実施:
  - `scripts/lib/referenceSignatureConfig.mjs` の watch list に `Range.HasSpill` / `SavedAsArray` / `SpillParent` を追加。
  - `docs/process/mslearn-signature-regeneration.md` に現在の watch list、owner 選定基準、`Range` 動的配列メンバーの更新導線を追記。
  - `TASKS.md` を更新し、本作業を完了済みに移し、次候補を `Application / Workbook / Worksheet` 系の未掲載監視候補整理へ更新。
- 残課題:
  - `Application` / `Workbook` / `Worksheet` 系で、個別ページはあるが現行スナップショットへ未掲載のメンバー候補を引き続き洗い出す。

## 2026-03-11 PR #49 feat: Workbook root alias から組み込みメンバーを解決
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `Application` / `Workbook` / `Worksheet` の owner inventory では object page 由来の未掲載 member は見つからず、既存参照データの到達性改善として `ActiveWorkbook` / `ThisWorkbook` / `Application.ActiveCell` の root alias 解決を追加した。
  - PR 前の `reviewer` 自己レビューでは、`ThisWorkbook.cls` fixture の未追跡と、`ThisWorkbook` alias に対する semantic token 分岐漏れが指摘され、どちらも修正した。
  - 初回 CodeRabbit は head commit 変更で失敗したため、`pause` 後に再レビューを実行した。再レビューでは 2 件の指摘が出て、どちらも採用して修正した。
- 指摘一覧:
  - [採用] extension / server テストで読む `ThisWorkbook.cls` fixture を追加し、PR 差分へ含めた。
  - [採用] `ThisWorkbook` document module と built-in alias が衝突する場合でも、semantic token では built-in alias を優先する分岐を追加した。
  - [採用] extension テストで `ThisWorkbook.cls` を開いたあと、`ThisWorkbook` ルートの definition が実際に `ThisWorkbook.cls` へ解決されるまで待ってから alias 系アサーションを実行するように修正した。
  - [採用] `ThisWorkbook` alias 判定を `symbol.kind === "module"` だけに依存させず、`VB_Base` と `VB_PredeclaredId = True` を持つ workbook document module に限定するよう厳密化した。
- この作業で当てはまりそうな内容（横展開候補）:
  - built-in alias を文書モジュールと両立させる場合、completion / hover / signature / semantic token の各入口で同じ shadowing ルールを適用しているかを横断確認したほうがよい。
  - fixture を新規追加したテスト変更では、コード差分だけでなく追跡状態も確認しないと、クリーン checkout 環境でだけ壊れる取りこぼしが起きやすい。
  - object page の inventory で未掲載が無かった owner でも、既存参照データへ到達できていない root alias が残っていないかは別軸で確認する価値がある。
  - document module 扱いの特例は、名前一致だけでなく host object を示す属性や module kind まで見ないと、同名の class module を誤分類しやすい。
- 実施:
  - `ActiveWorkbook` / `ThisWorkbook` に `Workbook` の `typeName` を付与し、`Application.ActiveCell` の chain でも `Range` member 解決を引き継げるようにした。
  - `ThisWorkbook.SaveAs` の completion / hover / semantic token、および `Application.ActiveCell.Address` の completion / signature / semantic token を server / extension テストへ追加した。
  - `TASKS.md` と `docs/process/mslearn-signature-regeneration.md` に owner inventory 結果と今回の到達性改善を反映した。
  - CodeRabbit 対応で `ThisWorkbook` document module 判定を `VB_Base + VB_PredeclaredId` ベースへ狭め、非 document class module では組み込み alias を有効化しない server テストを追加した。
  - extension テストは `ThisWorkbook` root の definition 解決待ちを追加し、built-in fallback だけで通る偽陽性を防いだ。
- 残課題:
  - 次候補は `Workbook / Worksheet callable` の signature help への昇格整理。

## 2026-03-13 PR #50 feat: Workbook callable の署名ヘルプを追加
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` に現在差分の事前自己レビューを依頼し、`Workbook.SaveAs` / `Workbook.Close` / `Workbook.ExportAsFixedFormat` の署名追加、`Void` returnType 保持、関連テスト、手順書更新の整合性を確認した。
  - 修正必須の指摘はなく、回帰リスク、境界条件、テスト不足、不要差分の観点で PR 化可能と判断された。
  - 初回 CodeRabbit では、`Workbook.Close` / `SaveAs` 系の parameter description に `workbook.If` / `Excel.NOTE:` のような文境界崩れが残っているという指摘が 1 件出た。
  - 指摘は妥当だったため、description 正規化を生成スクリプト側で補正し、script 監査テストへ再発防止を追加したうえで `@coderabbitai pause` 後に修正 push する。
- 指摘一覧:
  - [非採用] 指摘なし。`P0-P2` の修正必須事項は出なかった。
  - [採用] 生 markdown の parameter table に含まれる `sentence.If` / `Excel.NOTE:` 連結を生成時に正規化し、description へ単語間空白を補う。
- この作業で当てはまりそうな内容（横展開候補）:
  - Learn 由来の callable 追加では、signature help の増分だけでなく script 監査テストで `returnType` まで固定しておくと、後続 owner 追加時の欠落を早く検知できる。
  - `Sub` 相当の callable は、内部メタデータと UI 表示を分離して扱うと監査要件と既存 UX を両立しやすい。
  - `Workbook` と `Worksheet` のように候補が複数ある場合は、root 到達性が高い owner から先に昇格させるとテストと実利用の両方で効果が出やすい。
  - Microsoft Learn の markdown table は HTML タグだけでなく、文末 `.` の直後に次文が無空白で連結された状態を含むことがあるため、description 抽出では sentence boundary 正規化も監査対象に含めるとよい。
- 実施:
  - `scripts/lib/referenceSignatureConfig.mjs` に `Workbook` の allow list を追加した。
  - `scripts/generate-mslearn-vba-reference.mjs` で `Workbook.Close` / `SaveAs` / `ExportAsFixedFormat` の `returnType: "Void"` override とラベル抑止を追加した。
  - server / extension / script 監査テスト、および `TASKS.md` / 再生成手順書を更新した。
  - CodeRabbit 指摘対応として description 正規化で文境界空白を補い、`scripts/test/mslearnReferenceAudit.test.mjs` に collapsed sentence boundary の監査を追加した。
- 残課題:
  - `pause` 後の指摘修正 push を反映し、checks 緑化を確認してからマージする。

## 2026-03-13 PR #51 feat: Worksheet callable の署名ヘルプを追加
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは、indexed collection access の境界ケース、参照 JSON 差分範囲、server / extension テスト不足の観点で追加指摘は出なかった。
  - 初回 CodeRabbit では、`Worksheets(Array(...))` や関数呼び出し selector を単一 `Worksheet` と誤認する点、indexed-access helper の二重化、`SaveAs` / `ExportAsFixedFormat` の省略ラベル、`Workbooks.Open` typo が指摘された。
  - 指摘はいずれも妥当だったため採用し、selector 判定を保守化しつつ、shared helper 化、生成ラベル補正、説明文 typo 修正、監査テスト調整を行ったうえで `@coderabbitai pause` 後に修正 push する。
- 指摘一覧:
  - [非採用] `reviewer` 指摘なし。P0-P3 の追加修正事項は出なかった。
  - [採用] `Worksheets(Array(...))` を単一 `Worksheet` とみなさず、collection のまま扱う保守判定へ変更。
  - [採用] `packages/server/src/lsp/documentService.ts` の `stripIndexedAccessMarker()` 重複を廃止し、core の shared helper を利用。
  - [採用] `Workbook` / `Worksheet` の `SaveAs` と `ExportAsFixedFormat` の署名ラベルをフル引数リストへ変更。
  - [採用] `Workbooks.Open` の説明文 typo を生成スクリプト側で正規化。
  - [採用] `scripts/test/mslearnReferenceAudit.test.mjs` で、正当な `Object.Member` API 名を文境界欠落と誤判定しないように監査条件を補正。
- この作業で当てはまりそうな内容（横展開候補）:
  - collection selector の型昇格は、「単一要素アクセス」と「複数要素選択」を構文上区別できる範囲に限定したほうが誤補完を防ぎやすい。
  - semantic token 判定では string literal を scrub 済みテキストから消すと、selector 解析に必要な情報まで失うことがあるため、字句探索用と owner 解決用の入力を分けると安全。
  - 生成 JSON の監査では sentence boundary と API 名の `Object.Member` 表記を区別しないと、正当な typo 修正が false positive になりやすい。
  - 長い fixed-arity signature は `...` 省略よりフル label の方が signature help の実用性が高い一方、variadic 系まで一律に広げないよう owner / member 単位 override で制御した方がよい。
- 実施:
  - `skipTrailingIndexedAccess` を、文字列リテラルを保持したまま selector の複雑度を判定する形へ見直し、`Array(...)` と関数呼び出しは collection のまま残すようにした。
  - server / extension テストへ、`Worksheets(Array(...))` の負ケースと、`Worksheets("A(1)")` / `Worksheets(i + 1)` の正ケースを追加した。
  - `stripIndexedAccessMarker` を core から export し、server 側の二重実装を削除した。
  - 生成スクリプトで `SaveAs` / `ExportAsFixedFormat` の label override と `Workbooks.Open` 正規化を追加し、参照 JSON を再生成した。
  - script / server / extension を含む `npm test`、`npm run lint`、`npm run package` を再実行して通過を確認した。
- 残課題:
  - `Sheet1` のような worksheet document module alias を安全に `Worksheet` へ接続する条件整理は、次候補として継続する。

## 2026-03-13 PR #52 feat: worksheet document module root を解決
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは、重大な指摘（`P0-P2`）は出ず、回帰リスク、境界条件、テスト不足、不要差分の観点で PR 化可能と判断された。
  - `ThisWorkbook` 専用だった document module root 判定を一般化し、`VB_PredeclaredId=True` かつ `VB_Base=Worksheet GUID` の module だけを `Worksheet` root として昇格するようにした。
  - `Sheet1` の completion / signature help / semantic token の正常系と、非 worksheet predeclared class module の保守系を server / extension テストで固定した。
  - CodeRabbit からは `normalizeDocumentBaseGuid` の VBA 固有フォーマット説明コメント追加だけが指摘され、軽微修正として採用した。`@coderabbitai pause` 後に修正 push し、再レビュー待ちは行わない。
- 指摘一覧:
  - [非採用] `reviewer` 指摘なし。追加修正は不要と判断された。
  - [採用] `packages/server/src/lsp/documentService.ts` の `normalizeDocumentBaseGuid` に、`VB_Base` が `0{GUID}` 形式で保存される VBA 固有事情を説明するコメントを追加した。
- この作業で当てはまりそうな内容（横展開候補）:
  - document module root の特例は名前一致だけでなく `VB_Base` と `VB_PredeclaredId` を見ないと、同名 class module や他 host object を誤分類しやすい。
  - alias 到達性の追加では completion / signature help / hover / semantic token の入口ごとに別実装が残りやすいため、owner 判定 helper を共通化したほうが回帰を防ぎやすい。
  - worksheet 以外の host object root は、参照データと owner 判定条件が揃うまで保守動作に留めたほうが誤補完を抑えやすい。
- 実施:
  - `packages/core` に既知 owner から built-in member chain を辿る helper を追加した。
  - `packages/server` で workbook / worksheet document module の owner 判定を `VB_Base` GUID ベースへ整理した。
  - `Sheet1.cls` fixture と server / extension の回帰テストを追加し、`TASKS.md` を更新した。
  - CodeRabbit 対応として `normalizeDocumentBaseGuid` の意図コメントを追記した。
- 残課題:
  - Chart / DialogSheet など worksheet 以外の host object root の扱いは次候補で整理する。

## 2026-03-13 PR #53 feat: chart document module root を解決
- レビュー状況: `COMMENTED（対応済み・Reviews paused）`
- 要約:
  - `reviewer` の事前自己レビューでは、`Chart` document module の正常系だけでなく、誤って root 昇格してはいけない class module 側の負ケースを追加しておくべきだという指摘があり、`Chart2` と `DialogSheet1` の保守ケースを server テストへ追加してから PR 化した。
  - `VB_PredeclaredId=True` かつ `VB_Base=Chart GUID` の class module だけを `Chart` owner に昇格し、`Chart1.ChartArea` と `Chart1.SetSourceData` の completion / signature help / hover / semantic token を server / extension テストで固定した。
  - 初回 CodeRabbit では 1 件だけ actionable な指摘があり、否定ケースの semantic token 抑止テストで 0-based 行番号が stale になっていて、実際の `Chart1.Evaluate` / `DialogSheet1.ChartArea` 位置を検証できていない点が示された。
- 指摘一覧:
  - [採用] `packages/server/test/documentService.test.js` の semantic token 抑止テストで、`Chart1.Evaluate` の行番号期待値を `5 -> 6`、`DialogSheet1.ChartArea` の行番号期待値を `7 -> 10` へ修正した。
- この作業で当てはまりそうな内容（横展開候補）:
  - semantic token の負ケースで行番号と列番号を直接固定するテストは、fixture の追記だけで簡単に stale になるため、fixture 側の対象行をコメントで明示するか、位置算出 helper を使う方が安全。
  - CodeRabbit の status が `SUCCESS` でも outside-diff range comment に actionable な指摘が残ることがあるため、status context だけでなく review 本文も必ず確認する。
  - document module root の特例追加では、正常系の補完だけでなく、hover / signature / semantic token も含めた「保守動作」の否定ケースを 1 セットで固定した方が回帰を見つけやすい。
- 実施:
  - `packages/server/src/lsp/documentService.ts` に `Chart` document module 判定を追加し、`VB_Base` GUID 正規化を経由して `Chart` owner へ接続するようにした。
  - `packages/extension/test/fixtures/Chart1.cls` と各 fixture / server / extension テストを追加し、`Chart1` ルートの built-in member 解決を固定した。
  - CodeRabbit 対応として、否定ケースの semantic token 抑止テストの期待位置を現在の fixture 行番号へ合わせて更新した。
- 残課題:
  - `DialogSheet` の document module root をどの owner へ接続すべきかは未整理なので、次候補として継続する。

## 2026-03-13 PR #54 docs: DialogSheet document module の方針を整理
- レビュー状況: `REVIEW SKIPPED（path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは `P0-P3` の指摘は無く、`DialogSheet` を現時点では built-in owner へ昇格しない判断、根拠ソース、`TASKS.md` の更新内容は整合していると確認された。
  - CodeRabbit は `AGENTS.md` / `TASKS.md` / `docs/adr/0004-dialogsheet-document-module-policy.md` / `docs/process/mslearn-signature-regeneration.md` を path filters 対象として扱い、初回レビューは `Review skipped` だった。
  - そのため本 PR では、CodeRabbit の actionable comment は無く、`reviewer` 結果と公式ソース確認を根拠に docs-only の判断整理として完了扱いにした。
- 指摘一覧:
  - [非採用] `reviewer` 指摘なし。重大度付き findings は出なかった。
  - [非採用] CodeRabbit actionable comment なし。path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs-only PR でも、CodeRabbit status が `SUCCESS` かどうかだけでなく、最新の `coderabbitai` コメントで `Review skipped` 理由を確認してから完了扱いにする。
  - Office VBA object page が不足する owner は、即実装へ進まず、概念記事、参照 JSON、interop ページ、COM metadata を分けて整理してから ADR に落とす方が判断を共有しやすい。
  - interop page を補助ソース候補にする場合は、`Reserved for internal use` と `dummy` member の扱いを先にルール化しないと、生成データへノイズが混ざりやすい。
- 実施:
  - `docs/adr/0004-dialogsheet-document-module-policy.md` を追加し、`DialogSheet` document module を未公開のまま保守動作とする方針を記録した。
  - `docs/process/mslearn-signature-regeneration.md` に `DialogSheet` 調査結果を追記し、Office VBA / interop ソースの扱いを整理した。
  - `AGENTS.md` と `TASKS.md` に新 ADR への導線と次候補を反映した。
- 残課題:
  - `DialogSheet` interop page を補助ソースとして取り込むなら、owner 正規化方針と `dummy` member 除外規則を別タスクで固める必要がある。

## 2026-03-13 PR #55 docs: DialogSheet interop 補助ソースの可否を整理
- レビュー状況: `REVIEW SKIPPED（path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは `P0-P3` の指摘は無く、全面自動導入を見送る結論、限定導入時の制約、`TASKS.md` の次候補まで整合していると確認された。
  - CodeRabbit は `AGENTS.md` / `TASKS.md` / `docs/process/dialogsheet-interop-source-feasibility.md` / `docs/process/mslearn-signature-regeneration.md` を path filters 対象として扱い、初回レビューは `Review skipped` だった。
  - 本 PR では、DialogSheet interop を「全面導入は不採用、補助ソースとしての限定利用は可能」と整理し、次段階の最小プロトタイプで必要な allow list / skip rule / 監査テストを明文化した。
- 指摘一覧:
  - [非採用] `reviewer` 指摘なし。重大度付き findings は出なかった。
  - [非採用] CodeRabbit actionable comment なし。path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - Office VBA object page が不足する owner は、参照可否の有無だけでなく、discovery 経路、owner page の品質、member ノイズ量まで分けて評価した方が次の実装判断に使いやすい。
  - interop page を補助ソースにする場合は、owner 単位の allow list と `_Dummy*` / legacy member 除外規則を docs 先行で固めてから実装へ進む方が、生成 JSON へのノイズ混入を防ぎやすい。
  - docs-only PR が path filters で review skipped になっても、`reviewer` の結果と Microsoft Learn の一次ソース確認をレビュー要約へ残しておくと、後続タスクで判断根拠を再利用しやすい。
- 実施:
  - `docs/process/dialogsheet-interop-source-feasibility.md` を追加し、source inventory、現行生成器との相性、skip rule 候補、推奨方針を整理した。
  - `docs/process/mslearn-signature-regeneration.md` に新ドキュメントへの導線を追加した。
  - `AGENTS.md` と `TASKS.md` に調査メモへの導線、本タスク完了、次候補の最小プロトタイプを反映した。
- 残課題:
  - 次段階では `DialogSheet` common callable の最小プロトタイプを実装し、`dummy` / legacy member 除外と重複正規化の監査テストまで入れる必要がある。

## 2026-03-13 PR #56 feat: DialogSheet common callable の補助参照を追加
- レビュー状況: `SUCCESS`
- 要約:
  - `reviewer` の事前自己レビューでは重大指摘は無く、補助ソースの hard fail 方針、`DialogSheets(1)` / `DialogSheets.Item(1)` / `DialogSheets("Dialog1")` / `DialogSheets(Array(...))` の境界、既存 root への回帰テスト有無を確認した。
  - 追加の medium 指摘として、補助ソース生成失敗の負例テストと `typeName` 優先化の全体影響テストを厚くできる余地は出たが、現行の監査テストと既存 `Worksheets` 系回帰で今回差分の安全性は十分と判断した。
  - CodeRabbit は `No actionable comments were generated` で完了し、実装修正は不要だった。
- 指摘一覧:
  - [非採用] `reviewer`: `memberTypeOverrides` 未解決や補助ソース署名抽出失敗の負例テストを追加できる余地。
    理由: 生成器側で hard fail は既に実装済みで、今回の PR では生成後監査と user-facing 回帰を優先したため。
  - [非採用] `reviewer`: `createMemberReferenceItem` の `typeName` 優先化に対する追加の全体影響テスト。
    理由: `Worksheets` / `ThisWorkbook` / `Sheet1` / `Chart1` を含む既存 root 回帰は `npm test` で通過しており、今回の変更範囲では追加修正より既存カバレッジで十分と判断したため。
  - [非採用] CodeRabbit actionable comment なし。walkthrough と docstring coverage 警告のみ。
- この作業で当てはまりそうな内容（横展開候補）:
  - Office VBA 正本に owner page が無い object を補助ソースで導入する場合は、allow list、legacy member 除外、生成失敗条件、監査テストをセットで持つべき。
  - collection clone を導入する場合は、`Item` など返却型が owner 解決に効く member を `memberTypeOverrides` で明示し、grouped selector を単一 owner へ誤昇格させない境界テストを必ず置く。
  - `No actionable comments` の PR でも、CodeRabbit の walkthrough に含まれる要約は今後の似た変更の設計メモとして再利用できる。
- 実施:
  - `scripts/lib/supplementalReferenceConfig.mjs` を追加し、`DialogSheet` interop allow list と `DialogSheets` clone 設定を導入した。
  - `scripts/generate-mslearn-vba-reference.mjs` で supplemental source 取り込み、allow list 欠落・重複・署名抽出失敗・clone override 未解決の hard fail を追加した。
  - `packages/core/src/reference/builtinReference.ts` に `dialogsheets -> DialogSheet` 解決と JSON `typeName` 優先を追加した。
  - server / extension / script 監査テスト、および `TASKS.md` / `docs/process/mslearn-signature-regeneration.md` を更新した。
- 残課題:
  - `DialogSheets(1)` だけで十分か、`ActiveWorkbook.DialogSheets(1)` / `Application.DialogSheets(1)` まで広げるかは次候補で整理する。

## 2026-03-13 PR #57 feat: DialogSheets の Workbook/Application root を追加
- レビュー状況: `SUCCESS`
- 要約:
  - `reviewer` の事前自己レビューでは `P0-P3` の指摘は無く、`Application.DialogSheets` / `Workbook.DialogSheets` の `typeName: "DialogSheets"` 正規化、grouped selector の抑止、監査テスト追加、docs/TASKS の整合が確認された。
  - 非 blocking の補足として `ThisWorkbook.DialogSheets(1)` の専用回帰が薄いという指摘があり、PR 作成前に server / extension テストへ追加して取り込んだ。
  - CodeRabbit は `No actionable comments were generated` で完了し、実装修正は不要だった。docstring coverage 警告のみで、本 PR の対応対象外とした。
- 指摘一覧:
  - [採用] `reviewer`: `ThisWorkbook.DialogSheets(1)` の専用回帰テストを追加したほうが Workbook alias の退行を見つけやすい。
    理由: `Workbook.DialogSheets` 補助 property を追加した今回の差分と直接関係し、低コストで安全性を上げられるため。
  - [非採用] CodeRabbit actionable comment なし。walkthrough と docstring coverage 警告のみ。
- この作業で当てはまりそうな内容（横展開候補）:
  - interop property page が戻り値を広い型 (`Sheets`) で返していても、product 側でより狭い user-facing root (`DialogSheets`) へ正規化する場合は、理由を docs と監査テストの両方へ残したほうが後続レビューで迷いにくい。
  - `Application` / `ActiveWorkbook` / `ThisWorkbook` のような alias root 追加では、built-in root、document module alias、grouped selector 抑止を 1 セットで回帰固定したほうが安全。
  - CodeRabbit が `No actionable comments` の場合でも、selected files と ignored files をログへ残しておくと、path filters の影響範囲を後で追いやすい。
- 実施:
  - `scripts/lib/supplementalReferenceConfig.mjs` に `Application.DialogSheets` / `Workbook.DialogSheets` の補助 property 設定を追加した。
  - `scripts/generate-mslearn-vba-reference.mjs` に既存 owner へ補助 member を注入する処理を追加し、`resources/reference/mslearn-vba-reference.json` を再生成した。
  - `scripts/test`、`packages/server/test/documentService.test.js`、`packages/extension/test/suite/index.ts` に Application / ActiveWorkbook / ThisWorkbook 経由の回帰と grouped selector 抑止を追加した。
  - `docs/process/dialogsheet-interop-source-feasibility.md`、`docs/process/mslearn-signature-regeneration.md`、`TASKS.md` を更新した。
- 残課題:
  - `DialogSheet` 専用 control collection (`Buttons` / `CheckBoxes` / `OptionButtons` / `DialogFrame`) の補助参照可否は未整理のため、次候補として継続する。

## 2026-03-13 PR #58 docs: DialogSheet control collection の導入方針を整理
- レビュー状況: `REVIEW SKIPPED（path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは `P0-P3` の指摘は無く、`DialogFrame` を先行候補に絞る方針、`Buttons` / `CheckBoxes` / `OptionButtons` を docs 段階に留める切り分け、`TASKS.md` の次候補が妥当と確認された。
  - CodeRabbit は `TASKS.md`、`docs/process/README.md`、`docs/process/dialogsheet-control-collection-feasibility.md`、`docs/process/dialogsheet-interop-source-feasibility.md`、`docs/process/mslearn-signature-regeneration.md` を path filters 対象として扱い、最新コメントは `Review skipped due to path filters` だった。
  - 本 PR では、control collection を実装せず、direct property と selector 分岐の難しさを分離して、次段を `DialogFrame` 単独プロトタイプへ絞る判断を文書化した。
- 指摘一覧:
  - [非採用] `reviewer` 指摘なし。重大度付き findings は出なかった。
  - [非採用] CodeRabbit actionable comment なし。path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - interop 補助ソースの導入候補でも、direct property と `Optional Index As Object -> As Object` の collection method は別タスクに分けたほうが owner 設計を誤りにくい。
  - docs-only PR が path filters で skipped になっても、`reviewer` の結果と一次ソースから導いた結論は月次ログへ残しておくと次の実装 PR で再利用しやすい。
  - collection page の `Item` が `Object` を返す場合は、owner clone だけでなく `memberTypeOverrides` と grouped selector 抑止を先に設計してから実装へ進めたほうが安全。
- 実施:
  - `docs/process/dialogsheet-control-collection-feasibility.md` を追加し、公式ソース、owner 設計の難所、段階導入案を整理した。
  - `docs/process/mslearn-signature-regeneration.md` と `docs/process/dialogsheet-interop-source-feasibility.md` に補助導線を追加した。
  - `docs/process/README.md` と `TASKS.md` を更新し、本タスク完了と次候補 `DialogFrame` 最小プロトタイプを反映した。
- 残課題:
  - 次段では `DialogSheet.DialogFrame` を direct property として補助参照へ追加し、allow list と `_Dummy*` 除外の最小構成を code / test で固定する。

## 2026-03-14 PR #67 feat: worksheet control metadata probe を追加
- レビュー状況: `COMMENTED -> PAUSED / REVIEW SKIPPED`
- 要約:
  - `reviewer` の事前自己レビューでは `P0-P3` の修正必須指摘は無く、Open XML package から worksheet control metadata を抜く最小 probe、CLI、synthetic workbook package test、依存追加のスコープは妥当と確認された。
  - 初回 CodeRabbit では 3 件の actionable comment が付き、`control` rel を無条件に `ocx` part とみなして probe 全体を落としうる点、`toUnsignedInteger()` が `-1` や `3foo` を通してしまう点、`--out` に入力と同じパスを渡すと workbook を JSON で上書きできる点が指摘された。
  - これらはすべて採用し、`ctrlProp` type のときだけ `classId` を読むガード、不正 `shapeId` の reject、`--out` 同一パス拒否、`--out` 成功パスと失敗パス、非 `ctrlProp` rel、壊れた `shapeId` の test を追加した。
  - 指摘対応の push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメントは `Reviews paused.`、最新 status は `CodeRabbit: Review skipped` となった。
- 指摘一覧:
  - [採用] `scripts/lib/workbookControlMetadata.mjs`: `control` の relationship を型確認せずに `ocx` part とみなしていた。
    理由: worksheet 内の 1 control part が壊れているだけで probe 全体が失敗しうるため。`ctrlProp` type に限定し、読めない場合は `classId: null` へフォールバックした。
  - [採用] `scripts/lib/workbookControlMetadata.mjs`: `toUnsignedInteger()` が `parseInt()` のため `-1` や `3foo` を受理していた。
    理由: `shapeId` 結合の誤マッチを防ぐ必要があるため。非負整数文字列だけを許可し、それ以外は `null` にした。
  - [採用] `scripts/probe-workbook-control-metadata.mjs`: `--out` に入力ファイルと同じパスを指定すると workbook を破壊できた。
    理由: CLI 安全性として明確な破壊経路だったため。`path.resolve()` 比較で同一パスを拒否するようにした。
  - [採用] `scripts/test/workbookControlMetadata.test.mjs`: `--out` 成功パスの test が無かった。
    理由: file output 分岐は CI で見逃しやすいため。JSON 書き出しの内容検証を追加した。
- この作業で当てはまりそうな内容（横展開候補）:
  - workbook package probe のような補助 CLI は、「成功系の stdout」だけでなく `--out` の成功系と破壊的入力拒否も最初から test で固定した方が安全。
  - Open XML の relationship は target path だけでなく `Type` も見てから解釈しないと、part 方言差や壊れた package で全体失敗しやすい。
  - `shapeId` や line/column のような結合キーは、`parseInt()` で緩く読むより、許容形式を先に正規表現で絞った方が誤結合を防ぎやすい。
  - CodeRabbit 指摘だけを直す push では `@coderabbitai pause` を先に打ち、最新コメントが `Reviews paused.` と `Review skipped` になっていることまで確認してからマージ可否を判断する。
- 実施:
  - `scripts/lib/workbookControlMetadata.mjs` に `ctrlProp` relationship type ガード、`tryLoadActiveXClassId()`、厳格な `toUnsignedInteger()` を追加した。
  - `scripts/probe-workbook-control-metadata.mjs` に `--out` 同一パス拒否を追加した。
  - `scripts/test/workbookControlMetadata.test.mjs` に `--out` 書き出し成功、`--out` 同一パス拒否、非 `ctrlProp` rel、壊れた `shapeId` の回帰 test を追加した。
  - 検証として `npm run test:scripts`、`npm run lint`、`npm test`、`npm run package` を実行した。
- 残課題:
  - strict package や実ブック固有の Open XML 方言差まではまだ扱っていないため、sidecar schema 検討と並行して必要になった owner / relationship type を追加で整理する。

## 2026-03-14 PR #68 docs: sidecar artifact 仕様を整理
- レビュー状況: `REVIEW SKIPPED（path filters）`
- 要約:
  - `reviewer` の事前自己レビューでは `P2` と `P3` が 1 件ずつ出て、次候補に `probe -> sidecar v1 生成/配置` が入っていない点と、`controlType` の `CommandButton` が `DialogSheet.Button` と混同されやすい点が指摘された。
  - これらは両方採用し、`TASKS.md` の次候補へ sidecar 生成経路を追加し、新規正本 [worksheet-control-metadata-sidecar-artifact.md](../worksheet-control-metadata-sidecar-artifact.md) に worksheet ActiveX の `CommandButton` / `CheckBox` / `OptionButton` は `DialogSheet` 側 owner と別である注記を追記した。
  - その後の `reviewer` 再確認はサブエージェント側の環境エラーで差分確認まで到達しなかったが、修正必須だった P2/P3 は取り込み済みと判断した。
  - CodeRabbit は docs-only 差分のため `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/worksheet-chart-control-metadata-source-poc.md`、`docs/process/worksheet-control-metadata-sidecar-artifact.md` だった。
- 指摘一覧:
  - [採用] `reviewer`: 次候補に `probe -> sidecar v1 生成/配置` の経路が抜けている。
    理由: sidecar lookup だけ先に進めても、読むべき schema v1 sidecar が生成されずタスク接続が切れるため。
  - [採用] `reviewer`: `controlType` の `CommandButton` は `DialogSheet.Button` と混同しやすい。
    理由: worksheet ActiveX owner と DialogSheet owner の誤接続を防ぐため、正本に別 owner である注記を入れた。
  - [非採用] CodeRabbit actionable comment なし。path filters により review skipped。
- この作業で当てはまりそうな内容（横展開候補）:
  - docs で schema を固定するときは、lookup だけでなく「誰がその schema を生成するか」まで次候補へ接続しないと実装タスクが空振りしやすい。
  - 同名に見える UI control owner でも、`Worksheet/Chart` の ActiveX と `DialogSheet` の control collection は別 owner として注記しておく方が、後続の正規化テーブル実装で迷いにくい。
  - docs-only PR が path filters で skipped になった場合も、ignored files の一覧をレビュー要約へ残しておくと、CodeRabbit が何を見ていないかを後で追いやすい。
- 実施:
  - `docs/process/worksheet-control-metadata-sidecar-artifact.md` を追加し、schema v1、配置ルール、lookup 規則、未対応 owner 表現、現行 probe 出力との差分を整理した。
  - `docs/process/README.md` と `docs/process/worksheet-chart-control-metadata-source-poc.md` に導線を追加した。
  - `TASKS.md` に本タスク完了と次候補 `Worksheet control metadata sidecar lookup の最小実装` を反映し、生成経路を明示した。
- 残課題:
  - 次段では `probe -> sidecar v1 生成` と `nearest ancestor lookup` を実装し、初回は read-only cache と validation までで sidecar 読み込みを固定する必要がある。
## 2026-03-14 PR #69 feat: Worksheet control metadata sidecar lookup を追加
- レビュー結果: `COMMENTED` -> review fix 後 `SUCCESS`
- 指摘
  - `reviewer` は workspace root 境界と sidecar 変換の安全性を確認し、workspace root 未指定時の lookup 無効化、unknown `controlType` の fail-fast、cache reload テスト補強を求めた
  - CodeRabbit は `packages/server/src/lsp/documentService.ts` の sidecar cache 共有参照、`setWorkspaceRoots()` 後の stale state、`scripts/lib/worksheetControlMetadataSidecar.mjs` の `probe.version` 未検証、server/core/scripts テスト不足を指摘した
- 対応
  - `packages/server/src/lsp/documentService.ts` で sidecar state を readonly + freeze + defensive clone に変更し、cache は不変オブジェクト、`DocumentState` には複製を保持するようにした
  - `setWorkspaceRoots()` で cache クリア後に既存 `DocumentState` の `worksheetControlMetadata` を再解決するようにした
  - `scripts/lib/worksheetControlMetadataSidecar.mjs` で `probe.version !== 1` を fail-fast にし、`packages/server/test/documentService.test.js`、`packages/core/test/worksheetControlMetadataSidecar.test.ts`、`scripts/test/workbookControlMetadata.test.mjs` を補強した
  - `npm run lint` / `npm test` / `npm run package` を通した
- この作業で当てはまりそうな内容
  - sidecar / cache / parsed metadata は shared mutable reference を避け、cache は read-only、外部へ返す state は clone する
  - `version` を持つ変換 helper は出力 schema だけでなく入力 schema version も必ず検証する
  - workspace root や path 境界を変更できる API を追加したら、既存 state の invalidation / 再解決テストを同時に入れる
- 補足
  - `reviewer` 再レビューは環境エラー（`windows sandbox backend ... refusing to run unsandboxed`）で差分取得に失敗した

## 2026-03-15 PR #70 feat: OLEObject.Object に worksheet control metadata を接続
- レビュー結果: `COMMENTED` -> `@coderabbitai pause` 後 `REVIEW SKIPPED`
- 指摘
  - `reviewer` は初回差分で、`SheetA.OLEObjects("Control1").Object` の sidecar lookup が呼び出し元モジュール側ではなく root document module 側の bundle を見るべきだと指摘した
  - CodeRabbit は `packages/server/src/lsp/documentService.ts` に 2 件付き、`OLEObject.Object` が `builtinContext.rootState.worksheetControlMetadata` の stale snapshot を掴み続ける点と、chart document root に `worksheetControlOwnerKind: "chartsheet"` を載せると将来の sidecar で `Chart1.OLEObjects(...)` を誤解決しうる点を指摘した
- 対応
  - `DocumentModuleBuiltinContext` は `rootState` を持たず、`rootUri` と `rootModuleName` を持つ形へ変更し、sidecar owner 解決時に `getWorksheetControlMetadataState(rootUri)` で毎回 root 側の最新 sidecar を再取得するようにした
  - chart document root は built-in owner として `Chart` を返すまま、worksheet control sidecar 用の owner kind は持たせないようにして、chartsheet owner を sidecar 解決対象から除外した
  - `packages/server/test/documentService.test.js` に sidecar-only regeneration 後の owner 切替回帰を追加し、既存の named selector テストでは `supported` な `chartsheet` owner を置いても `Chart1.OLEObjects("CheckBox1").Object` が未解決のまま維持されることを固定した
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - document module の補助コンテキストで sidecar / metadata を参照する場合は、解析 snapshot 全体を保持せず、`uri` や module 名のような stable identity だけを保持して最新 state を再取得したほうが stale bug を埋め込みにくい
  - `Chart` / `DialogSheet` のように将来データが来る可能性がある root でも、現時点で未対応なら `ownerKind` を割り当てない形で保守的に閉じ、synthetic sidecar を使った境界テストで固定したほうが安全
  - sidecar 系の回帰は「コード再解析なしで sidecar だけ差し替える」ケースを 1 本入れておくと、cache と snapshot の取り違えを見つけやすい
  - 事前自己レビューと CodeRabbit が同じ論点を別角度から指摘した場合は、片方だけで済ませず、再読込経路と境界テストまで含めて修正したほうが後戻りしにくい

## 2026-03-15 PR #71 feat(sidecar): worksheet control code name 解決を追加
- レビュー結果: `COMMENTED` -> `@coderabbitai pause` 後 push 済み
- 指摘
  - `reviewer` は 4 件出し、負例 hover テストが初回空配列で即成功して偽陽性になり得る点、chartsheet 方針の文書不整合、`OLEObjects` 方針の文面自己矛盾、sidecar 未検出時の未解決回帰不足を指摘した
  - CodeRabbit は `packages/server/src/lsp/documentService.ts` に 1 件だけ付き、`controlSegment.text` ではなく既に切り出している `controlSegmentName` を比較キーに使う一貫性を求めた
- 対応
  - `packages/extension/test/suite/index.ts` に `waitForNoHover()` を追加し、`Sheet1.CheckBox1` / `Chart1` / `ActiveSheet` の負例 hover を複数回の空結果で確認するようにした
  - `packages/server/test/documentService.test.js` に sidecar 未検出時は `Sheet1.chkFinished.Value` / `Select` が未解決のまま維持される回帰を追加した
  - `docs/process/worksheet-control-metadata-sidecar-artifact.md` と `docs/process/worksheet-chart-control-identity-feasibility.md` の文言を揃え、chartsheet は supported/unsupported を問わず未解決、`OLEObjects` は既定では `OLEObject` までだが sidecar あり string literal / code name の限定例外を許可する方針へ明文化した
  - CodeRabbit 指摘は採用し、`resolveWorksheetControlOwnerFromCodeName()` の照合キーを `controlSegmentName` に統一した
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - `hover が出ないこと` を待つ test は、`waitForHover(..., hovers.length === 0)` のような初回成功条件にせず、一定回数連続で空を確認する helper を分けたほうが遅延提供の偽陽性を防ぎやすい
  - sidecar ありの正例を追加したときは、sidecar 未検出時の同一入力も 1 本固定しておくと、lookup 漏れや accidental fallback を見逃しにくい
  - docs の境界条件は `supported/unsupported`、`既定`、`限定例外` の語を揃えておくと、TASKS と正本の判断メモがずれにくい
  - CodeRabbit の nit でも、既に導出した正規化キーや local variable があるならそちらへ寄せて比較軸を統一したほうが読みやすさとレビュー容易性が上がる

## 2026-03-15 PR #72 feat(reference): Shapes root の Shape surface を追加
- レビュー結果: `COMMENTED` -> `@coderabbitai pause` 後 `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは `P0-P3` の指摘は無く、`Shapes(Index)` / `Shapes.Item(Index)` を generic `Shape` へ進め、`Shape.OLEFormat.Object` は昇格させない境界が実装・テスト・docs で整合していることを確認した
  - 初回 CodeRabbit は 2 件付いた。1 件目は `Chart1.Shapes.Item(1)` の正例と `Shapes(GetIndex())` / `Shapes.Item(GetIndex())` の負例が extension/server test に不足しているという指摘、2 件目は `Shapes.Item.typeName` を `Shape` に変えるべきという指摘だった
- 対応
  - 1 件目は採用し、`packages/server/test/documentService.test.js` に `Chart1.Shapes.Item(1)` の正例を追加し、`packages/extension/test/fixtures/ShapesBuiltIn.bas` と `packages/extension/test/suite/index.ts` に `Shapes(GetIndex())` / `Shapes.Item(GetIndex())` の collection 維持を確認する completion / hover / semantic token の負例を追加した
  - 2 件目は非採用とし、`resources/reference/mslearn-vba-reference.json` の `typeName: "Shapes"` は `OLEObjects.Item` と同じく collection owner 名を保持し、`INDEXED_COLLECTION_OWNER_TYPES` の access-kind 判定で `literal` / `single` のときだけ `Shape` へ降ろすために必要だと返信で理由を残した
  - review 修正 push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメントが `Reviews paused.` / `Review skipped` になったこと、`quality-check` / `danger-check` / `label` / `CodeRabbit` が最新 head で `SUCCESS` であることを確認した
- この作業で当てはまりそうな内容
  - collection の `Item` を collection owner 名で保持して access-kind で item owner へ降ろす設計では、見かけ上の戻り型だけを見たレビュー指摘が入りやすいので、PR か返信で「なぜ collection 名なのか」を残しておくと再説明コストが下がる
  - 正例を追加した PR では、function selector や grouped selector の負例を extension E2E まで入れておくと、「only この経路だけ昇格する」という境界を壊しにくい
  - review fix を `pause` 運用で流す場合でも、最新の `coderabbitai` コメントが `Reviews paused.` まで更新されたことを確認しないと、古い actionable review だけ見て状態を誤認しやすい
- 実施
  - `packages/core/src/reference/builtinReference.ts` に `shapes` の indexed collection owner map を追加した
  - `scripts/lib/supplementalReferenceConfig.mjs` と `resources/reference/mslearn-vba-reference.json` を更新し、`Shapes.Item` の override を追加した
  - `packages/server/test/documentService.test.js`、`packages/extension/test/fixtures/ShapesBuiltIn.bas`、`packages/extension/test/suite/index.ts`、`scripts/test/mslearnReferenceAudit.test.mjs` を追加・更新した
  - `docs/process/worksheet-chart-shapes-root-feasibility.md`、`docs/process/worksheet-chart-control-entrypoint-feasibility.md`、`docs/process/README.md`、`TASKS.md` を更新した
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行した
- 残課題
  - 次段では `Shape.OLEFormat.Object` を control owner へ昇格させる条件整理と、`ShapeRange` / grouped selector の扱いを別タスクで詰める必要がある

## 2026-03-15 PR #73 docs(policy): Shape.OLEFormat.Object の昇格条件を整理
- レビュー結果: `NO ACTIONABLE COMMENTS` -> review log 追記後 `@coderabbitai pause`
- 指摘
  - `reviewer` は `P3` として、`ShapeRange` / grouped selector を昇格対象外にする判断が docs にだけあり、server / extension test に負例が不足している点を指摘した
  - CodeRabbit の最新レビューコメントは `No actionable comments were generated` で、selected files は `packages/extension/test/fixtures/ShapesBuiltIn.bas`、`packages/extension/test/suite/index.ts`、`packages/server/test/documentService.test.js`、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/shape-oleformat-object-promotion-feasibility.md`、`docs/process/worksheet-chart-shapes-root-feasibility.md`、`docs/process/worksheet-control-metadata-sidecar-artifact.md` だった
  - CodeRabbit の pre-merge warnings として、PR description がテンプレート必須セクションを満たしていない点と docstring coverage warning が出たが、今回の差分に対する actionable code review ではなかった
- 対応
  - `packages/server/test/documentService.test.js` に `Sheet1.Shapes.Range(Array("CheckBox1")).OLEFormat.Object.` の completion 負例を追加し、`Value` が出ないことを固定した
  - `packages/extension/test/fixtures/ShapesBuiltIn.bas` と `packages/extension/test/suite/index.ts` に `Sheet1.Shapes.Range(Array("CheckBox1")).OLEFormat.Object.Value` の hover / semantic token 負例を追加した
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行し、`CodeRabbit` / `quality-check` / `danger-check` / `label` の通過を確認した
- この作業で当てはまりそうな内容
  - docs に「除外する」と書いた境界条件は、同じ PR のうちに server / extension の少なくとも片側へ負例を追加しないと、設計メモだけが先行して regression guard が抜けやすい
  - `ShapeRange` のように未対応 surface を別 task 扱いにするときも、既存 resolver がそこへ誤って進まないことを negative test で先に固定しておくと、次段の限定昇格実装を安全に進めやすい
  - CodeRabbit が actionable comment なしでも、selected / ignored files と pre-merge warnings をレビュー要約へ残しておくと、「何を見て何を見ていないか」を後から追跡しやすい

## 2026-03-15 PR #74 feat: Shape.OLEFormat.Object の限定解決
- レビュー結果: `COMMENTED` -> `@coderabbitai pause` 後 `SUCCESS`
- 指摘
  - `reviewer` は blocking 指摘なしだったが、任意強化として `PlainShape` の sidecar 不一致負例と `Worksheets("Sheet1")` root の負例を足すと境界が明確になると提案した
  - 初回 CodeRabbit は 3 論点を出した。1 件目は `packages/server/src/lsp/documentService.ts` で `.Object` の下位 access まで誤って昇格し得る点、2 件目は extension test で `.Item("CheckBox1")` 側の `Select` detail 検証と shape 専用 member の負例が不足している点、3 件目は `.Item("PlainShape")` / `Worksheets("Sheet1").Shapes.Item("CheckBox1")` の負例が不足している点だった
  - CodeRabbit の selected files は `packages/extension/test/fixtures/ShapesBuiltIn.bas`、`packages/extension/test/suite/index.ts`、`packages/server/src/lsp/documentService.ts`、`packages/server/test/documentService.test.js` で、docs と `TASKS.md` は path filters により ignored だった
- 対応
  - `reviewer` の補足提案は採用し、初回 commit の時点で `Sheet1.Shapes("PlainShape").OLEFormat.Object` と `Worksheets("Sheet1").Shapes("CheckBox1").OLEFormat.Object` の負例を server / extension test に追加した
  - CodeRabbit 1 件目は採用し、`resolveWorksheetControlOwnerFromSidecar()` で `.Object` セグメント自身の `accessKind === "none"` を必須にして、`Sheet1.Shapes("CheckBox1").OLEFormat.Object(1)` のような下位 access を sidecar 昇格対象から除外した
  - CodeRabbit 2 件目は採用し、extension test に `itemShapeObjectSelectCompletion` の detail 検証を追加し、負例を `Activate` ではなく `Delete` に変えて shape 専用 member 混入を検知するようにした
  - CodeRabbit 3 件目は採用し、`.Item("PlainShape")`、`Worksheets("Sheet1").Shapes.Item("CheckBox1")`、`Sheet1.Shapes("CheckBox1").OLEFormat.Object(1)` の hover / completion 負例を server / extension test に追加した
  - review 修正 push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメントが `Reviews paused.`、push 後の CodeRabbit 状態が `SUCCESS` / `Review skipped` であることを確認した
  - 検証として初回・修正後ともに `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - `.Object` のような generic member を限定昇格させるときは、owner 名だけではなく「そのセグメント自体が raw access か」を条件に入れないと、`.Object(1)` や default member 呼び出しまで誤解決しやすい
  - 正例を `Shapes("...")` と `Shapes.Item("...")` の両方に広げたら、負例も同じ 2 経路でそろえないと `.Item(...)` 側だけ条件が緩んだ regressions を見逃しやすい
  - `Shape` / `OLEObject` / control owner が近接している導線では、負例は「存在しないこと」を見るだけでなく、どの owner の member を排除したいかに合わせて `Delete` のような owner 特有 member を選んだほうが判定精度が上がる

## 2026-03-21 PR #80 feat(server): active workbook identity snapshot transport を追加
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause`
- 指摘
  - `reviewer` は初回に 2 件出し、`available` snapshot / `workbook-binding.json` が docs 契約より緩く、`isAddin=true` や `path=""` を `match` 扱いできてしまう点と、placeholder `unavailable` により `binding-disabled` log が解析ごとに増える点を指摘した。修正後の再レビューでは `TASKS.md` の placeholder 記述だけ追加で指摘され、最終的に `P0-P3` 残件なしを確認した
  - CodeRabbit は 3 論点を出した。1 件目は `normalizeWorkbookFullNameForComparison()` が `process.platform` 依存で、非 Windows 実行時に Windows path 比較を誤る点、2 件目は `workbookBindingManifest.ts` の `isAddIn` 検証が二段階でメッセージがぶれる点、3 件目は同種の Windows path 比較 helper を local path lookup にも横展開できる点だった
  - CodeRabbit の nit として `switch (state)` の `default` を exhaustiveness check にした方がよい提案もあったが、runtime validation で先に不正 state を reject しており、今回の transport 境界修正とは独立なため見送った
- 対応
  - `packages/core/src/reference/activeWorkbookIdentity.ts` で `available` は `non-addin + non-empty path` のみ受理し、`unsupported` では `reason=addin` / `reason=unsaved` と identity 値の整合も確認するようにした
  - `packages/core/src/reference/workbookBindingManifest.ts` で manifest は `saved + non-addin` のみ `loaded` 扱いとし、`workbook.isAddIn` は `false の boolean` であることを 1 回の validation で扱うように整理した
  - `packages/server/src/lsp/documentService.ts` で `active-workbook-identity.binding-*` log を URI ごとに重複抑止し、同じ状態での再解析ログを増やさないようにした
  - `packages/extension/src/extension.ts` は起動時 placeholder snapshot を送らず、host bridge 実装時にだけ `vba/activeWorkbookIdentity` を流す形へ変更した
  - CodeRabbit の Windows path 論点は当てはまりそうな箇所へ横展開し、`normalizeWorkbookFullNameForComparison()` は常に Windows path として lower-case 比較し、`workbookBindingManifest.ts` と `worksheetControlMetadataSidecar.ts` の local path helper には Windows-style path を検出したときだけ `path.win32.resolve(...).toLowerCase()` を使う補正を入れた
  - `packages/core/test/activeWorkbookIdentity.test.ts`、`packages/core/test/workbookBindingManifest.test.ts`、`packages/server/test/documentService.test.js` を追加・更新し、invalid payload、invalid manifest、binding log 重複抑止を回帰固定した
  - 検証として修正前後ともに `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - Excel / VBA 起源の identity path は「サーバーの OS」ではなく「Windows path であること」を基準に正規化した方が、remote / container 実行でも比較規則がぶれにくい
  - review で出た path 正規化の論点は、同じ repo 内の類似 helper にも当てはまるかを必ず確認し、今回のように manifest lookup と sidecar lookup へ横展開しておくと再発しにくい
  - runtime state を未初期化で持てる設計なら、placeholder event を無理に送るより「未受信」と「unavailable」を分けた方が運用ログと host 未実装期間の挙動が安定する
  - review fix で `pause` 運用に入る場合でも、fix 前の latest `coderabbitai` コメント内容と selected / ignored files を要約へ残しておくと、後から「何を見て何を見ていないか」を説明しやすい

## 2026-03-21 PR #86 test: broad root family のテスト補助を整理
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは `P0-P3` 指摘なしで、`matched` / `mismatched` / `unavailable` / shadow / root `.Item` 非対象の broad root 境界は整理後も維持され、変更は 3 ファイルに閉じていると確認された
  - CodeRabbit は `packages/server/test/documentService.test.js` に 2 件出し、1 件目で `mismatched` / `unavailable` の検証粒度が `closed` / `matched` より粗い点、2 件目で `createWorksheetBroadRootFixture()` が生成途中例外で temp dir を残しうる点を指摘した
- 対応
  - 1 件目は採用し、server 側 broad root テストで `mismatched` / `unavailable` でも completion / hover / signature と非対象 selector の否定ケースを配列駆動で揃えた
  - 2 件目は採用し、`createWorksheetBroadRootFixture()` を `try/catch` で包み、fixture 生成途中に失敗しても `rmSync(..., { force: true, recursive: true })` で cleanup するようにした
  - 修正後に `npm test` と `npm run package` を再実行し、push 前に `@coderabbitai pause` を実行した。最新の `coderabbitai` コメントは `Reviews paused.`、最新 status は `CodeRabbit: Review skipped` を確認した
- この作業で当てはまりそうな内容
  - テスト整理 PR でも、状態行列を helper 化したときは `matched` だけでなく `mismatched` / `unavailable` も同じ粒度へ寄せた方が、将来の回帰を取りこぼしにくい
  - temp directory を作る test fixture helper は、戻り値の `cleanup()` だけに頼らず、生成途中失敗時の cleanup も helper 内で保証した方が安全
  - CodeRabbit の nit でも、タスクの目的と直接噛み合うなら small follow-up として取り込んだ方が PR の完成度を上げやすい

## 2026-03-21 PR #87 feat: unqualified broad root の root Item 形式を解決
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは `P0-P3` 指摘なしで、`Worksheets.Item("SheetName")` / `Application.Worksheets.Item("SheetName")` を direct call form と同じ broad root family に入れる実装、server / extension test、ADR / process doc / `TASKS.md` の整合が取れていることを確認した。補足として、過去の `root .Item` 非対象タスクが歴史上残るため `superseded` 注記を追加すると誤読しにくいと提案した
  - CodeRabbit は `packages/extension/test/suite/index.ts` に 3 件付き、1 件目で `Application.Worksheets.Item("Sheet One").OLEObjects...` と `Worksheets.Item("Sheet One").Shapes...` の対称ケースが不足している点、2 件目で unavailable 側 no-hover / no-signature の assert message が不足している点、3 件目で `Worksheets.Item(1)` / `Worksheets.Item(GetIndex())` / `Application.Worksheets.Item(GetIndex())` の非 literal 境界が不足している点を指摘した
- 対応
  - `packages/server/src/lsp/documentService.ts` に `resolveWorksheetsRootSelector()` を追加し、`Worksheets("Sheet")` / `Worksheets.Item("Sheet")` / `Application.Worksheets("Sheet")` / `Application.Worksheets.Item("Sheet")` を同じ sheetName selector family として扱うようにした
  - `packages/server/test/documentService.test.js` で root `.Item("SheetName")` の broad root 正例を server 側へ追加し、manifest match / mismatch / unavailable / shadow の既存境界を維持することを固定した
  - `packages/extension/test/fixtures/WorksheetBroadRootBuiltIn.bas` と `packages/extension/test/suite/index.ts` に `Application.Worksheets.Item("Sheet One").OLEObjects...`、`Worksheets.Item("Sheet One").Shapes...`、`.Item("CheckBox1")` 対称ケース、`Worksheets.Item(1)` / `Worksheets.Item(GetIndex())` / `Application.Worksheets.Item(1)` / `Application.Worksheets.Item(GetIndex())` の負例を追加し、no-hover / no-signature assert に message を渡すようにした
  - `docs/adr/0005-explicit-sheet-name-root-policy.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`TASKS.md` を更新し、旧来の `root .Item` 非対象判断は superseded 済みであることと、次候補が workbook-qualified root `.Item("SheetName")` 形式の整理であることを明記した
  - 検証として初回実装時に `npm run lint`、`npm test`、`npm run package` を実行し、CodeRabbit 修正後に `npm test`、`npm run package` を再実行した
- この作業で当てはまりそうな内容
  - direct call form と `.Item("literal")` form を同じ family として扱うなら、正例も負例も `Worksheet` / `Application`、`OLEObjects` / `Shapes` の直交ケースを対称にそろえないと、片側だけ条件が緩む regressions を見逃しやすい
  - `no hover` / `no signature` 系の assert は token ごとの message を必ず渡したほうが、配列駆動 test の triage が速い
  - broad root の selector 境界は literal 正例を開いたときに、numeric / dynamic selector の `.Item(...)` も同じ PR で負例固定しておくと過剰解決を抑えやすい
  - 以前の docs 判断を後続タスクで覆した場合は、単に削除せず `superseded` 注記を残したほうが履歴追跡と `TASKS.md` の読みやすさが上がる

## 2026-03-21 PR #88 feat(server): workbook-qualified な Worksheets.Item root の解決を追加
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` 予定
- 指摘
  - `reviewer` は初回に 2 件出し、`ActiveWorkbook.Worksheets.Item("Sheet1")` の codeName 指定と `.Item(1)` numeric selector について、server / extension ともに completion / hover / signature help の負例が不足している点を指摘した。修正後の再レビューでは、extension 側の semantic token 負例が `ActiveWorkbook` 分で不足している点だけを追加で指摘した
  - CodeRabbit は 2 論点を出した。1 件目は extension test の semantic token 負例に重複 assert がある点、2 件目は workbook-qualified root `.Item` テストで `server` 側に semantic token の非対象境界が無く、また extension 側の closed / matched hover・signature が `.Item` / 非`.Item` で非対称な点だった
- 対応
  - `packages/server/src/lsp/documentService.ts` で workbook-qualified root の sidecar lookup を `resolveWorksheetsRootSelector()` ベースに寄せ、`Worksheets.Item("SheetName")` を direct call form と同じ sheetName selector family として扱うようにした
  - `packages/server/test/documentService.test.js` と `packages/extension/test/suite/index.ts` に、`ThisWorkbook.Worksheets.Item("Sheet One")` の正例、`ActiveWorkbook.Worksheets.Item("Sheet One")` の gating 正例、`ActiveWorkbook.Worksheets.Item("Sheet1")` / `.Item(1)` の completion / hover / signature help / semantic token 負例を追加した
  - CodeRabbit 指摘は採用し、extension 側の重複 `assertNoDecodedSemanticToken()` を削除し、closed / matched hover・signature を `.Item` / 非`.Item` で対称化したうえで、server 側にも snapshot 状態ごとの semantic token 負例を追加した
  - `docs/adr/0005-explicit-sheet-name-root-policy.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`docs/process/workbook-binding-manifest-feasibility.md`、`TASKS.md` を更新し、`.Item("SheetName")` 対応後の broad root / workbook binding 文言不整合を解消した
  - 検証として `npm run lint`、`npm test`、`npm run package` を実行し、CodeRabbit 修正後に `npm run test --workspace @vba/server`、`npm run test --workspace vba-extension` を再実行した
- この作業で当てはまりそうな内容
  - direct call form と `.Item("literal")` form を同一 family として開く場合、正例だけでなく codeName / numeric selector の負例を completion / hover / signature help / semantic token まで一気通貫で固定しないと、表示系だけ緩む regressions を見逃しやすい
  - `.Item` / 非`.Item` を同時サポートするテストは、completion だけ対称でも hover / signature が片側だけ欠けやすいので、vector を横に並べて見る review が有効
  - 配列駆動で semantic token 負例を追加するときは、line index が 0-based か 1-based かを先に確認しないと、今回のように「存在しない identifier を見に行く」だけのノイズを生みやすい

## 2026-03-21 PR #89 docs(root): Application workbook root 方針を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件だけ出て、`docs/process/explicit-sheet-name-broad-root-feasibility.md` と `docs/process/workbook-binding-manifest-feasibility.md` の「既に開いている経路」列挙が網羅に見える一方で、実装済みの一部パターンが抜けている点を指摘した
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/adr/0005-explicit-sheet-name-root-policy.md`、`docs/process/README.md`、`docs/process/application-workbook-root-feasibility.md`、`docs/process/explicit-sheet-name-broad-root-feasibility.md`、`docs/process/workbook-binding-manifest-feasibility.md` だった
- 対応
  - `docs/process/application-workbook-root-feasibility.md` を追加し、`Application.ThisWorkbook.Worksheets("SheetName")` / `.Item("SheetName")` は `ThisWorkbook` direct root と同じ static current-bundle family、`Application.ActiveWorkbook.Worksheets("SheetName")` / `.Item("SheetName")` は `ActiveWorkbook` direct root と同じ manifest + snapshot gating family として扱う方針を整理した
  - ADR `0005-explicit-sheet-name-root-policy.md` を更新し、`Application` qualifier を挟んでも workbook root identity の意味は変えないこと、built-in `Application` として解決できる場合にだけ sidecar lookup を有効にする shadow 境界を追記した
  - `reviewer` 指摘は採用し、broad-root / binding メモの列挙に「代表例」である旨を追記して、未実装経路と実装済み経路の誤読を防ぐ文言に修正した
  - `docs/process/README.md` と `TASKS.md` を更新し、関連メモの導線と次タスクを `Application.ThisWorkbook` / `Application.ActiveWorkbook` root の最小接続へ進めた
- この作業で当てはまりそうな内容
  - docs で user-facing 経路を列挙するときは、網羅ではなく代表例ならその旨を明記しないと、実装済みパターンの抜けが policy の欠落に見えやすい
  - `Application.` qualifier を挟む派生 root は、direct root と別物に見えやすいが、一次情報上の意味が同じなら family を先に固定してから実装へ進めた方が docs と code のズレを減らせる
  - docs-only PR でも CodeRabbit の ignored files を残しておくと、「レビューが無かった」のではなく「path filters で見ていない」ことを後から説明しやすい

## 2026-03-21 PR #90 feat(server): Application workbook root の接続を追加
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件出て、`Application.ThisWorkbook` / `Application.ActiveWorkbook` を開く正例はそろっている一方、`Worksheets(GetIndex())` の dynamic selector 負例が `Application.*Workbook` 経路で抜けている点を指摘した
  - CodeRabbit は 2 論点を出し、1 件目で `packages/server/test/documentService.test.js` の `Application.ThisWorkbook` 検証に `ThisWorkbook` document module を含む fixture が必要な点、2 件目で direct root `ActiveWorkbook("x").Worksheets(...)` が indexed-access を除外せず `Workbook` context を返しうる点を指摘した
- 対応
  - `reviewer` 指摘は採用し、`packages/server/test/documentService.test.js`、`packages/extension/test/suite/index.ts`、`packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas` に `Application.ThisWorkbook.Worksheets(GetIndex())...` と `Application.ActiveWorkbook.Worksheets(GetIndex())...` の completion / hover / signature help / semantic token 負例を追加した
  - CodeRabbit の 1 件目は採用し、server 側の `Application.ThisWorkbook` / `Application.ActiveWorkbook` テストを `createWorksheetBroadRootFixture()` から `createWorkbookQualifiedWorksheetRootFixture()` ベースへ切り替えて `ThisWorkbook` document module 前提を満たすようにした
  - CodeRabbit の 2 件目も採用し、`packages/server/src/lsp/documentService.ts` の direct `ActiveWorkbook` broad-root 分岐に `accessKind === "none"` ガードを追加し、`packages/server/test/documentService.test.js` に `ActiveWorkbook("Book1").Worksheets.Item("Sheet One")...` が閉じたままである専用負例テストを追加した
  - review fix push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメントが `Reviews paused.` であること、push 後の `CodeRabbit` status context が `SUCCESS` のまま `Review skipped` になっていることを確認した
  - 検証として `npm run build`、`npm test --workspace @vba/server`、`npx tsc -p packages/extension/tsconfig.test.json`、`npm run lint`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - workbook root family を広げるときは、literal 正例と同時に dynamic / numeric / codeName selector の負例を direct root、qualified root、`Application.` qualifier 付き root まで対称にそろえた方が過剰解決を抑えやすい
  - built-in document root を前提にしたテストは、broad-root 用 fixture を流用せず `ThisWorkbook` や document module を含む専用 fixture を使った方が shadow や root 判定の偽陽性を防ぎやすい
  - `stripIndexedAccessMarker()` を通る root family は、`rootSegment` の文字列比較だけでなく `MemberAccessPathSegment.accessKind` まで見ないと `Foo("x")` と `Foo` を取り違えやすい

## 2026-03-21 PR #91 refactor: workbook root family の resolver と matrix を整理
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件だけ出て、`Application.ThisWorkbook` / `Application.ActiveWorkbook` 経路の整理後も `Application.Caller` と `Application.Range("A1")` が workbook root family に誤接続しないことを、server / extension の負例として固定した方がよいと指摘した
  - CodeRabbit は 2 論点を出し、1 件目で `packages/extension/test/suite/index.ts` の definition warm-up が `ThisWorkbook` 単独 token を見ており、将来 fixture に別の `ThisWorkbook` 出現が増えると誤位置を取る点、2 件目で `packages/server/src/lsp/documentService.ts` の type-only `Application` 無視条件が workbook-root family より広く、`Application.Caller` や `Application.Range("A1")` まで generic built-in fallback に流しうる点を指摘した
  - CodeRabbit の selected files は `packages/extension/test/suite/index.ts` と `packages/server/src/lsp/documentService.ts` で、issue comment 側の walkthrough では `packages/server/test/documentService.test.js` と `packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas` も差分要約対象に含まれていた
- 対応
  - `reviewer` 指摘は採用し、`packages/server/test/documentService.test.js`、`packages/extension/test/suite/index.ts`、`packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas` に `Application.Caller.OLEObjects(...)` と `Application.Range("A1").Shapes(...).OLEFormat.Object` の completion / hover / signature help / semantic token 負例を追加した
  - CodeRabbit 1 件目は採用し、extension test の definition warm-up を `findPositionAfterToken(..., "Application.ThisWorkbook", -1)` に変更して、guard 対象を full root token に固定した
  - CodeRabbit 2 件目も採用し、`packages/server/src/lsp/documentService.ts` の type-only `Application` 無視は「無視した結果として実際に workbook root family の built-in context が成立する場合」にだけ通すよう絞り込み、`Application.Caller` / `Application.Range("A1")` は引き続き未解決のまま維持した
  - 追加で、closed / unavailable broad-root matrix の no-hover / no-signature 検証を full matched array から導出するよう `packages/extension/test/suite/index.ts` を整理し、`Shapes` 側だけ assertion 粒度が緩む drift を防いだ
  - review 修正 push 前に `@coderabbitai pause` を実行し、最新の `coderabbitai` コメントが `Reviews paused.`、status context が `CodeRabbit: SUCCESS` / issue comment が `Review skipped` であることを確認した
  - 検証として修正後に `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - definition warm-up や cursor 位置固定は短い token 名より full root token を使った方が、fixture 追加で同名識別子が増えたときの誤ヒットを防ぎやすい
  - `Application` のように built-in と type / enum shadow が競合する root は、「shadow を無視する条件」を先に一般化しすぎず、実際に狙った root family が成立したときだけ fallback させた方が安全
  - matched / mismatched / unavailable の状態行列を helper 化した後でも、closed 側の no-hover / no-signature / no-definition を同じ元配列から導出しないと、片側だけ assertion 粒度が緩む regressions を埋め込みやすい

## 2026-03-21 PR #92 test: workbook root family の semantic helper を整理
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは重大指摘なしで、semantic token helper 化そのものは妥当という確認だった。CodeRabbit 対応後の差分再レビューでも重大指摘なしだった
  - CodeRabbit は 2 論点を出し、1 件目で `packages/extension/test/suite/index.ts` の `Application.*` anchor が `Demo()` と `ShadowedApplication()` に重複しているのに `occurrenceIndex` が無く、shadowed `Application` の semantic token 負例が未検証な点を指摘した
  - CodeRabbit の 2 件目は `packages/server/test/documentService.test.js` の `staticNoSemanticChecks` / `matchedNoSemanticCases` などで negative semantic matrix が薄く、codeName / numeric / dynamic selector や `Application.Caller` / `Application.Range("A1")` の `.Value` / `.Select` が抜けていて回帰を見逃しうる点を指摘した
- 対応
  - `packages/extension/test/suite/index.ts` の `assertWorkbookRootNoSemanticCases()` 呼び出しに、`Application.ThisWorkbook.Worksheets("Sheet One").OLEObjects("CheckBox1").Object.Value` と `Application.ActiveWorkbook.Worksheets("Sheet One").Shapes("CheckBox1").OLEFormat.Object.Value` の shadowed occurrence 向け `occurrenceIndex = 1` を追加し、static / matched の両状態で shadowed `Application` が semantic token を出さないことを固定した
  - `packages/server/test/documentService.test.js` の `Application.ThisWorkbook` / `Application.ActiveWorkbook` に関する OLE / Shape 両テストで、`staticNoSemanticChecks` と `matchedNoSemanticCases` を補強し、codeName / numeric / dynamic selector、`Application.Caller`、`Application.Range("A1")` の `.Value` と `.Select` を negative semantic matrix に追加した
  - 検証として `npm run lint`、`npm run test --workspace @vba/server`、`npm run test --workspace vba-extension`、`npm run package` を実行した。extension test の既知 warning `Overlapping semantic tokens detected at lineNumber 73, column 1` は継続したが、exit code は 0 のままだった
- この作業で当てはまりそうな内容
  - anchor token ベースの semantic token テストへ移すときは、同じ文字列が fixture 内で複数回出るケースを必ず洗い出し、`occurrenceIndex` か専用 anchor を付けないと shadow / duplicate 由来の regressions を見逃しやすい
  - completion / hover / signature help の負例を残していても、semantic token だけは別配列で drift しやすいので、codeName / numeric / dynamic / non-target root の negative matrix を review 時に横並びで照合した方がよい
  - `.Value` だけの負例では method 名 token の過剰色付けを取り逃すため、`.Select` のような function token も同じ family で 1 本ずつ押さえておくと回帰検知が安定する

## 2026-03-21 PR #93 docs(test): workbook root family の case table 方針を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは重大指摘なしで、server / extension の helper は package-local のまま維持し、shared 化対象を canonical case spec に限定する整理は妥当という確認だった
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/workbook-root-family-case-table-policy.md` だった
- 対応
  - `docs/process/workbook-root-family-case-table-policy.md` を追加し、repo root `test-support/` 配下の `*.cjs` を canonical case spec の正本候補とし、server / extension は package-local adapter を維持する方針を固定した
  - shared 化対象を fixture anchor / state / expectation kind / `occurrenceIndex` に絞り、`CompletionItem.detail` fragment、async wait 条件、failure message は package-local に残す判断を明文化した
  - `docs/process/README.md` に workbook root family test 系の導線を追加し、`TASKS.md` では方針整理タスクを完了、次タスクを `workbook root family の shared case spec を最小抽出する` へ更新した
- この作業で当てはまりそうな内容
  - JS / TS と同期 / 非同期 assertion が混在するテスト共通化は、helper まで 1 本化しようとすると build や型の都合で逆に見通しが悪くなるため、case spec と adapter の責務分離を先に固定した方が安全
  - anchor token ベースの matrix を shared 化するときは、`occurrenceIndex` を shared spec 側で持たないと shadow / duplicate anchor の regressions を package ごとに別の形で埋め込みやすい
  - docs-only の方針整理でも、次の最小実装単位を `TASKS.md` で具体化しておくと、そのまま次 PR を起こしやすい

## 2026-03-22 PR #95 test: workbook root family の hover / signature shared spec を追加
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件出て、shared 化後の server 側 `Application.ActiveWorkbook` OLE matrix で numeric / dynamic selector の no-hover / no-signature coverage が弱くなっている点を指摘した
  - CodeRabbit は 3 論点を出し、1 件目で `packages/server/test/documentService.test.js` の `mapSharedWorkbookRootInteractionCases()` が `occurrenceIndex` を返しているのに `assertWorkbookRootHoverCases` / `assertWorkbookRootNoHoverCases` / `assertWorkbookRootSignatureCases` / `assertWorkbookRootNoSignatureCases` が 2 要素しか受けず、重複 anchor の occurrence-aware 検証が無効化されている点を指摘した
  - CodeRabbit の 2 件目は `packages/extension/test/suite/index.ts` と `test-support/workbookRootFamilyCaseTables.cjs` に対して、`worksheetBroadRoot.signature.negative` とそれを読む `assertWorkbookRootNoSignatureCases` 経路が shared 化の途中で落ちており、`Sheets(...)` / `ActiveSheet` / numeric / dynamic selector の no-signature 回帰が未検証になっている点を指摘した
  - CodeRabbit の 3 件目は `test-support/workbookRootFamilyCaseTables.cjs` の `applicationWorkbookRoot.signature.negative` matched 側で code-name / numeric selector の no-signature anchor が不足しており、`Application.ActiveWorkbook.Worksheets.Item("Sheet1")...` / `.Item(1)...` の回帰を shared table から検証できない点を指摘した
- 対応
  - `reviewer` 指摘は採用し、`packages/server/test/documentService.test.js` の `Application.ActiveWorkbook` OLE fixture text に matched negative selector を補強したうえで、shared table 側も `applicationWorkbookRoot.signature.negative` matched entries を code-name / numeric / dynamic selector までそろえた
  - CodeRabbit の 1 件目は採用し、`packages/server/test/documentService.test.js` の hover / no-hover / signature / no-signature helper を 3 要素 tuple 対応にして、`getHoverAfterToken()` / `getSignatureHelpAfterToken()` へ `occurrenceIndex` を渡すように修正した
  - CodeRabbit の 2 件目も採用し、`test-support/workbookRootFamilyCaseTables.cjs` に `worksheetBroadRoot.signature.negative` を追加し、`packages/extension/test/fixtures/WorksheetBroadRootBuiltIn.bas` に対応する non-target `.Select(` anchor を補充、`packages/extension/test/suite/index.ts` で `broadRootNonTargetSignatureChecks` を生成して `assertWorkbookRootNoSignatureCases()` に流すようにした
  - CodeRabbit の 3 件目も採用し、`packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas` と server 側の埋め込み text に `Application.ActiveWorkbook.Worksheets.Item("Sheet1").OLEObjects("CheckBox1").Object.Select(` / `.Item(1)...Select(` を追加し、extension / server の両方で shared negative signature entries を実 anchor 付きで検証できるようにした
  - 追加で、`packages/extension/test/suite/index.ts` の no-active-workbook / mismatch snapshot 向け re-map でも `occurrenceIndex` を捨てないようにして、shared interaction tuple の情報欠落を防いだ
  - review 修正 push 前に `@coderabbitai pause` を実行し、push 後に最新の `coderabbitai` コメントが `Reviews paused.`、`CodeRabbit` status context が `Review skipped` になっていることを確認した
  - 検証として `npm run test --workspace @vba/server`、`npx tsc -p packages/extension/tsconfig.test.json`、`npm run lint`、`npm run package` を実行した。`npm run test --workspace vba-extension` はこの Codex セッション自身が VS Code instance を保持しているため `Running extension tests from the command line is currently only supported if no other instance of Code is running.` で実行不可だった
- この作業で当てはまりそうな内容
  - shared case spec を tuple 化したら、helper 側の destructuring が旧 2 要素のまま残っていないかを最初に総点検しないと、`occurrenceIndex` を持っていても重複 anchor regressions を見逃しやすい
  - hover / signature を shared table へ寄せると、positive だけ先に移って negative が抜けやすいので、`completion` / `hover` / `signature` の positive / negative 行列を family ごとに横並びで review した方が安全
  - shared table に anchor を追加するときは、spec だけでなく server 埋め込み text と extension fixture の実 anchor まで同時にそろえないと、scope filter 上は存在しても実テストでは未参照の dead entry を作りやすい

## 2026-03-22 PR #96 docs(test): shadow occurrence 差分の方針を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは重大指摘なしで、shadow hover / signature の occurrence 差分を policy doc に戻し、shared spec schema を v1 で広げない判断も妥当という確認だった
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md` と `docs/process/workbook-root-family-case-table-policy.md` だった
- 対応
  - `docs/process/workbook-root-family-case-table-policy.md` を更新し、extension の shadow hover は duplicate `.Value` anchor のため `occurrenceIndex = 1`、extension の shadow signature と server shadow fixture は `occurrenceIndex = 0` のままでよいことを明文化した
  - v1 では `occurrenceIndexByScope` / `occurrenceIndexByKind` のような per-scope override を shared spec schema に追加せず、shadow hover / signature は package-local のまま維持する判断と再評価トリガーを追記した
  - `TASKS.md` では該当タスクを完了化し、次候補を `workbook root family の shadow fixture topology をそろえる条件を整理する` に更新した
- この作業で当てはまりそうな内容
  - shared spec の schema を増やす前に、fixture topology の違いで local に残しているのか、assertion shape の違いで local に残しているのかを文書で切り分けておくと、不要な DSL 化を防ぎやすい
  - docs-only PR でも CodeRabbit の ignored files を残しておくと、「レビュー対象外だっただけ」であることを後から説明しやすい
  - `TASKS.md` の次候補は、今回 fixed した policy をそのまま次の問いに接続する形で書くと、判断メモと実装候補が分離しにくい

## 2026-03-22 PR #97 docs(test): shadow fixture topology の条件を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは重大指摘なしで、shadow fixture topology の候補比較と第一候補の固定は妥当という確認だった
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/workbook-root-shadow-fixture-topology-feasibility.md` だった
- 対応
  - `docs/process/workbook-root-shadow-fixture-topology-feasibility.md` を追加し、extension の mixed fixture と server の shadow 専用 inline fixture の差、`per-scope occurrence override` 不採用理由、extension 側 shadow 専用 fixture 分離を第一候補とする判断を整理した
  - `docs/process/README.md` に workbook root family test 系メモとして追加し、`TASKS.md` では該当タスクを完了化、次候補を `workbook root family の shadow 専用 fixture 分離 PoC を整理する` に更新した
- この作業で当てはまりそうな内容
  - shared spec 化の前段として fixture topology を比較するときは、単に「file を増やすか」ではなく、duplicate anchor の数、section 分割、server inline text との整合まで並べておくと判断が安定しやすい
  - docs-only PR の skip でも ignored files を残しておくと、CodeRabbit が見ていない範囲を次の判断メモへ持ち越しやすい
  - 次候補を PoC 単位まで落とすと、方針整理メモから実際のコード変更へ移るときの PR 境界を切りやすい

## 2026-03-22 PR #98 docs(test): shadow 専用 fixture 分離 PoC を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件だけ出て、ルートに未追跡の `.codex-pr-body-dialogframe.md` があるため、今回の docs-only 差分を `git add` で明示的に限定し、無関係なファイルをコミット対象へ含めないよう指摘した
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/coderabbit-review-logs/2026-03.md`、`docs/process/workbook-root-shadow-fixture-split-poc.md` だった
- 対応
  - 差分の修正は不要と判断し、`docs/process/workbook-root-shadow-fixture-split-poc.md`、`docs/process/README.md`、`TASKS.md`、この review log だけを明示 staging する前提で PR 作成へ進める
  - PR 作成後に最新の `coderabbitai` コメントと `CodeRabbit: SUCCESS` を確認し、skip が path filters 起因であることを review log に残した
- この作業で当てはまりそうな内容
  - docs-only PR でも未追跡ファイルが残っている場合は、`git add .` を避けて staging 対象を固定しないと無関係差分を混ぜやすい
  - docs-only PR では `CodeRabbit: SUCCESS` でも実質レビューは `Review skipped due to path filters` のことがあるため、status context だけでなく最新コメントまで残す必要がある

## 2026-03-22 PR 作成前 test: shadow 専用 fixture 分離を最小実装
- レビュー結果: `reviewer` 実行済み
- 指摘
  - 初回 `reviewer` では 2 件出て、extension 側 `text` filter によって anchor 欠落を見逃しうる点と、shadow semantic の空 token 待機が早すぎる点を指摘した
  - 再確認では、上記 2 件は解消済みで、未追跡 `.codex-pr-body-dialogframe.md` をコミット対象へ混ぜないことだけを残件として指摘した
- 対応
  - extension 側の `text` filter 追加は取り下げ、shadow semantic negative を `state: "shadowed"` へ寄せて shared spec 側で fixture 衝突を解消した
  - `packages/extension/test/suite/index.ts` に `waitForStableNoSemanticTokens()` を追加し、shadow document の no-semantic 判定を空 token 3 回連続確認へ変更した
  - `packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas` に shared spec との不足 anchor を追加し、main / shadow fixture どちらも shared entry と 1 対 1 で対応するように揃えた
  - PR では今回の tracked files だけを明示 staging し、無関係な未追跡ファイルは含めない
- この作業で当てはまりそうな内容
  - fixture を分離したあとに `text.includes(anchor)` で shared entry を落とすと、差分が欠けても「一部だけ残って通る」状態を作りやすいので、まず shared spec 側の state / scope を見直す方が安全
  - no-semantic の検証は `tokens.data.length === 0` を 1 回見るだけでは不安定で、少なくとも複数回連続確認か、事前の解析待ちを入れた方がよい

## 2026-03-22 PR #99 test: shadow 専用 fixture 分離を最小実装
- レビュー結果: `COMMENTED` -> 修正後 `@coderabbitai pause` / `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 2 件出て、extension 側 `text` filter による anchor 欠落の見逃しと、shadow semantic の空 token 待機が早すぎる点を指摘した
  - CodeRabbit は 2 件の nit を出し、1 件目で `packages/extension/test/suite/index.ts` の `waitForStableNoSemanticTokens()` が timeout 時に non-empty token を返すため失敗理由が曖昧な点、2 件目で `applicationWorkbookMatchedShadowHoverChecks` / `applicationWorkbookMatchedShadowSignatureChecks` が `entry.anchor.includes("ThisWorkbook")` に依存しており brittle な点を指摘した
  - CodeRabbit の selected files は `packages/extension/test/fixtures/ApplicationWorkbookRootBuiltIn.bas`、`packages/extension/test/fixtures/ApplicationWorkbookRootShadowed.bas`、`packages/extension/test/suite/index.ts`、`packages/server/test/documentService.test.js`、`test-support/workbookRootFamilyCaseTables.cjs` で、ignored files は `TASKS.md`、この review log、`docs/process/workbook-root-family-case-table-policy.md`、`docs/process/workbook-root-shadow-fixture-split-poc.md`、`docs/process/workbook-root-shadow-fixture-topology-feasibility.md` だった
- 対応
  - `reviewer` と CodeRabbit で共通していた shadow semantic 待機の論点は採用し、文書全体が空 token になるまで待つのではなく、shadow 対象 anchor の `no semantic token` 条件を満たすまで decode + assertion を再試行する `waitForNoSemanticTokensByCases()` へ差し替えた
  - timeout 時は `document`、`latestTokenCount`、最後に失敗した assertion message を含めて fail-fast するようにし、extension test の失敗理由を直接読める形へした
  - `entry.anchor.includes("ThisWorkbook")` は採用せず、`test-support/workbookRootFamilyCaseTables.cjs` の shadow hover / signature entry に `rootKind` を追加し、extension 側 message builder は `entry.rootKind` で分岐するように修正した
  - review 修正 push 前に `@coderabbitai pause` を実行し、push 後に最新の `coderabbitai` コメントが `Reviews paused.`、status context が `CodeRabbit: SUCCESS` / issue comment が `Review skipped` であることを確認した
  - 検証として修正後に `npm run lint`、`npm test`、`npm run package` を実行した
- この作業で当てはまりそうな内容
  - no-semantic 系の待機は「文書全体が空 token か」ではなく、「そのテストで禁止した anchor に token が付かないか」を直接判定しないと、無関係 token が増えたときに helper 自体が壊れやすい
  - failure を後段 assertion に任せると timeout 原因が見えにくくなるため、wait helper 側で最後の失敗内容を保持して fail-fast した方がデバッグしやすい
  - shared spec の説明文や test message を anchor 文字列の部分一致へ依存させると、fixture の文字列変更で壊れやすいので、`rootKind` のような意図を表す metadata を持たせた方が安全

## 2026-03-22 PR #100 docs(test): shadow text source canonical 化要否を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは P0-P3 指摘なしで、`shadow text source canonical 化は現時点不要` という結論を新しい正本文書へ寄せつつ、既存の方針文書と `TASKS.md` の導線を同期できていることを確認した
  - CodeRabbit は `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/workbook-root-family-case-table-policy.md`、`docs/process/workbook-root-shadow-fixture-split-poc.md`、`docs/process/workbook-root-shadow-text-source-canonicalization-feasibility.md` だった
- 対応
  - [docs/process/workbook-root-shadow-text-source-canonicalization-feasibility.md](../workbook-root-shadow-text-source-canonicalization-feasibility.md) を追加し、canonical 化対象は full text ではなく shared anchor spec に留め、server inline shadow text と extension dedicated shadow fixture の二重管理は当面維持する判断を記録した
  - [docs/process/workbook-root-family-case-table-policy.md](../workbook-root-family-case-table-policy.md) と [docs/process/workbook-root-shadow-fixture-split-poc.md](../workbook-root-shadow-fixture-split-poc.md) を、新しい正本文書を参照する導線に更新した
  - [docs/process/README.md](../README.md) と `TASKS.md` を更新し、この判断へ入口から辿れるようにした
  - docs-only のため build / lint / test は実行していない
- この作業で当てはまりそうな内容
  - shared spec がすでに anchor の canonical source になっている場合、full text source まで canonical 化するかは「実際に drift が review 主因か」を見てから決めた方が過剰設計を避けやすい
  - docs-only PR では CodeRabbit が path filters で skip しやすいため、status context だけでなく ignored files を記録しておくと、レビュー未実施範囲を後から説明しやすい
