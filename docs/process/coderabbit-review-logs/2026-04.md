# 2026-04 CodeRabbit Review Log

## 2026-04-01 PR #114 docs(test): worksheet control の shared spec 候補 family を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 1 件指摘があり、`OleObjectBuiltIn.bas` / `ShapesBuiltIn.bas` に broad root 正本が同居しているように読める記述を修正した。broad root の canonical source は [WorksheetBroadRootBuiltIn.bas](../../../packages/extension/test/fixtures/WorksheetBroadRootBuiltIn.bas) と [workbookRootFamilyCaseTables.cjs](../../../test-support/workbookRootFamilyCaseTables.cjs) に残ることを明記した
  - CodeRabbit の最新 `coderabbitai` コメントは初回 run の `Review skipped due to path filters` のままで、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/worksheet-control-shared-spec-family-candidate-feasibility.md` の 3 件だった。レビュー記録追記後の docs-only push では新しい `coderabbitai` コメントは付かなかった
- 対応
  - [worksheet-control-shared-spec-family-candidate-feasibility.md](../worksheet-control-shared-spec-family-candidate-feasibility.md) を追加し、最初の shared spec 候補 family を `worksheetControlShapeNamePath` に絞り、`shapeName` path と `codeName` path、broad root、chartsheet、generic surface の境界を整理した
  - broad root の正本は既存 `worksheetBroadRoot` family に残ることを [TASKS.md](../../../TASKS.md) にも追記し、次タスクを `worksheet control shapeName path の vocabulary と canonical anchor source を整理する` に更新した
  - [README.md](../README.md) に導線を追加し、docs-only のため build / lint / test は実行していない
- この作業で当てはまりそうな内容
  - shared spec 候補 family を切る段階では、resolver route より先に canonical anchor source の所在を固定しないと、既存 family と新候補 family の正本境界が崩れやすい
  - `shapeName` path と `codeName` path のように terminal owner が同じでも join key が違う系統は、最初から同一 family にまとめるより vocabulary を分けてから後続で再評価した方が review しやすい
  - docs-only PR で CodeRabbit が skip した場合でも、ignored files を残しておくと「bot 未レビューの判断メモ」がどれかを次タスクで追いやすい

## 2026-04-01 PR #115 docs(test): worksheet control shapeName path の語彙を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 3 点指摘があり、`matched/closed` を別 `state` 軸に見せないよう v1 では `rootKind` 側に残す前提を明記し、`reason` の見出しを selector 限定から負例全体へ修正し、[TASKS.md](../../../TASKS.md) の次タスクにも `scopes` と `state 軸を追加しない` 前提を追記した
  - CodeRabbit の最新 `coderabbitai` コメントはレビュー記録 push 後に更新された `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/coderabbit-review-logs/2026-04.md`、`docs/process/worksheet-control-shape-name-path-vocabulary-feasibility.md` の 4 件だった
- 対応
  - [worksheet-control-shape-name-path-vocabulary-feasibility.md](../worksheet-control-shape-name-path-vocabulary-feasibility.md) を追加し、`worksheetControlShapeNamePath` の v1 語彙を `rootKind=document-module/workbook-qualified-static/workbook-qualified-matched/workbook-qualified-closed`、`routeKind=ole-object/shape-oleformat`、negative `reason=numeric-selector/dynamic-selector/code-name-selector/plain-shape/chartsheet-root/non-target-root` に固定した
  - family canonical anchor source は単独 fixture ではなく、将来の `test-support/worksheetControlShapeNamePathCaseTables.cjs` のような dedicated case spec を前提にし、[OleObjectBuiltIn.bas](../../../packages/extension/test/fixtures/OleObjectBuiltIn.bas) と [ShapesBuiltIn.bas](../../../packages/extension/test/fixtures/ShapesBuiltIn.bas) は route-local execution source のまま残す方針を整理した
  - [README.md](../README.md) に導線を追加し、[TASKS.md](../../../TASKS.md) は今回タスクを完了へ移して、次タスクを `worksheet control shapeName path の dedicated case spec 抽出方針を整理する` に切り替えた
  - docs-only のため build / lint / test は実行していない
- この作業で当てはまりそうな内容
  - shared spec 候補 family の vocabulary を固定するときは、`rootKind` / `reason` / `scopes` の境界だけでなく、既存 family policy とぶつかる `state` 軸を暗黙に増やしていないかを先に確認した方がよい
  - route-local fixture を family canonical source にしない判断では、「単独 file で両 route を表せないこと」と「generic surface が同居して主語がぼやけること」を分けて書くと、後続の dedicated case spec 設計へつなげやすい
  - docs-only PR で CodeRabbit が skip した場合でも、reviewer 指摘を先に取り込んでから記録へ残すと、「bot 未レビューだが自己レビュー済みの判断」として後続で追いやすい

## 2026-04-01 PR #116 docs(process): worksheet control shapeName path の case spec 抽出方針を整理
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 3 点指摘があり、次タスクの `completion` 最小 PoC の完了条件が曖昧な点、`fixture` の値形式が `base name` と相対 path で揺れていた点、`v1 正本` と `正本候補` が family 文書間で混在していた点を修正した
  - 再レビューでは追加指摘なしを確認し、agent はクローズした
  - CodeRabbit の最新 `coderabbitai` コメントは review log 追記後に更新された `Review skipped due to path filters` で、ignored files は `TASKS.md`、`docs/process/README.md`、`docs/process/coderabbit-review-logs/2026-04.md`、`docs/process/worksheet-control-shape-name-path-case-spec-feasibility.md`、`docs/process/worksheet-control-shape-name-path-vocabulary-feasibility.md` の 5 件だった
- 対応
  - [worksheet-control-shape-name-path-case-spec-feasibility.md](../worksheet-control-shape-name-path-case-spec-feasibility.md) を追加し、dedicated case spec を family 単位の `test-support/worksheetControlShapeNamePathCaseTables.cjs` に置き、初回抽出は `completion` に限定する方針を整理した
  - `fixture` を repo root 基準の相対 path に固定し、`completion` 最小 PoC は両 route、positive の `document-module/static/matched`、negative の各 `reason` と各 route の `workbook-qualified-closed`、両 fixture、`extension` と server scope を含むことを受け入れ条件として明記した
  - [worksheet-control-shape-name-path-vocabulary-feasibility.md](../worksheet-control-shape-name-path-vocabulary-feasibility.md) の `v1 正本` 表現を今回の判断に合わせて更新し、[README.md](../README.md) と [TASKS.md](../../../TASKS.md) の導線と次タスクを同期した
  - docs-only のため build / lint / test は実行していない
- この作業で当てはまりそうな内容
  - dedicated case spec の抽出方針を決める段階でも、次タスクの完了条件を曖昧にすると reviewer と CodeRabbit の観点が「方針」ではなく「未定義の実装範囲」へ流れやすい
  - `fixture` のような common field は、許容値の形式まで固定しないと adapter 側の解釈差が後から schema 論点として再発しやすい
  - 過去の family 文書を更新せずに新文書だけ `v1 正本` へ寄せると、同じ family 内で「候補」と「確定」が混在して review の主語がぶれやすい

## 2026-04-01 PR #117 docs(process): 実装優先の作業方針を更新
- レビュー結果: `REVIEW SKIPPED`
- 指摘
  - `reviewer` の事前自己レビューでは 2 件の `P1` と 1 件の `P2` があり、`docs-only` 例外と Done 定義の矛盾、`worker` 定義と役割制約の衝突、実装優先ルールの重複記載を修正した
  - 再レビューでは追加指摘なしを確認し、agent はクローズした
  - CodeRabbit の最新 `coderabbitai` コメントは review log 追記後に更新された `Review skipped due to path filters` で、ignored files は `AGENTS.md`、`TASKS.md`、`docs/process/README.md`、`docs/process/coderabbit-review-logs/2026-04.md`、`docs/process/sub-agent-escalation.md` の 5 件だった
- 対応
  - [AGENTS.md](../../../AGENTS.md) に実装優先ルール、通常タスクの Done 定義、`TASKS.md` 更新時の扱いを追加し、正本をここへ寄せた
  - [TASKS.md](../../../TASKS.md) は `AGENTS.md` を正本参照に寄せ、進行中タスクを `完了` に移す条件だけを短く残した
  - [README.md](../README.md) と [sub-agent-escalation.md](../sub-agent-escalation.md) は参照中心に整理し、サブエージェント役割を調査、最小限 docs update 点抽出、差分レビュー補助に限定した
  - docs-only のため build / lint / test は実行していない
- この作業で当てはまりそうな内容
  - 実装優先ルールのような共通方針は、1 つの正本へ寄せて他文書は参照に留めた方が、後続の rule 変更で drift しにくい
  - `docs-only` の例外を入れるときは、Done 定義側でも「通常タスク」と「明示的な docs/運用ルール依頼」を分けて書かないと矛盾しやすい
  - サブエージェント運用制約は、利用可能な agent 種別の列挙よりも「このリポジトリで何に使うか」を優先して書いた方が実運用に合いやすい

## 2026-04-01 PR #118 test: worksheet control shapeName path の completion case spec を抽出
- レビュー結果: `ACTIONABLE COMMENTS 2`
- 指摘
  - CodeRabbit は `test-support/worksheetControlShapeNamePathCaseTables.cjs` の `workbook-qualified-closed` negative 2 件で `reason` 欠落を指摘し、`closed-workbook` を明示して schema を揃えるよう求めた
  - CodeRabbit は [documentService.test.js](../../../packages/server/test/documentService.test.js) の route 別 shared-case test 2 本が重複している点を指摘し、共通ランナー化を提案した
  - 追加の inline comment として、extension 側の OLE route shared completion assert 前に `ACTIVE_WORKBOOK_UNAVAILABLE_SNAPSHOT` を明示セットすること、`createWorksheetControlShapeNamePathFixture` の `readFileSync` を cleanup 保証の `try` 内へ移すことが出た
- 対応
  - [worksheetControlShapeNamePathCaseTables.cjs](../../../test-support/worksheetControlShapeNamePathCaseTables.cjs) の closed negative 2 件に `reason: "closed-workbook"` を追加し、[worksheetControlShapeNamePathCaseTables.test.mjs](../../../scripts/test/worksheetControlShapeNamePathCaseTables.test.mjs) で「すべての negative entry が reason を持つ」ことを監査した
  - [documentService.test.js](../../../packages/server/test/documentService.test.js) の route 別 test を `runWorksheetControlShapeNamePathCompletionSharedCases(...)` へ寄せ、fixture cleanup も `readFileSync` を含めて `try/finally` 管理に統一した
  - [index.ts](../../../packages/extension/test/suite/index.ts) は OLE route shared completion assert 前にも `ACTIVE_WORKBOOK_UNAVAILABLE_SNAPSHOT` を明示セットし、shared spec の前提状態を shape route と揃えた
  - 検証は `npm run test:scripts`、`npm run test --workspace @vba/server`、`npm run lint`、`npm run package` を通した。`npm run test --workspace vba-extension` は desktop session 上で 124 秒タイムアウトし、E2E は別途 VS Code instance 制約を切り分ける前提で保留とした
- この作業で当てはまりそうな内容
  - shared case spec を導入した直後は「closed / unavailable のような state 由来 negative」に `reason` を付け忘れやすいので、schema 一貫性は scripts 側で監査した方が再発しにくい
  - route ごとに fixture だけ違って制御フローが同じ test は、shared spec 導入時点で runner へ寄せた方が review comment の横展開コストが低い
  - extension test で shared snapshot 前提を置く場合は、前段 test の状態に依存しないよう route ごとの最初の assert 前で明示 reset した方が安全
